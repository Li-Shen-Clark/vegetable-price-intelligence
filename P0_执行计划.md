# P0 执行计划：数据层

对应 GUIDEBOOK.md §9 的 P0 阶段。
编制日期：2026-09-11
状态：**P0 前置项 A1–A4、T1–T8、T5C 与 T8L 全部完成（2026-09-12 00:45 EDT）**

---

## 执行管理规则

从 T0 开始，所有任务统一采用“先计划、后执行、再留痕”的顺序：

1. **计划先行**：执行前先在本文件写明任务编号、目标、输入、输出、修改范围、非目标、验收标准与回退方式。
2. **按计划执行**：未写入计划的变更不顺带实施；发现需要扩展范围时，先暂停并补写计划。
3. **执行后记录**：完成或中止后，立即在 `P0_执行日志.md` 追加实际动作、变更文件、验证结果、偏差、问题与下一步。
4. **状态分离**：计划中的 `planned / in_progress / completed / blocked` 与工作清单中的审核状态分别维护，不能用“已计划”代替“已完成”。
5. **日志只追加**：历史日志不覆盖；如需纠正，追加一条更正记录并引用原记录。
6. **逐步放行**：上一步未达到验收标准，不进入下一步。

### T0 执行卡（执行前记录）

| 项目 | 内容 |
|---|---|
| 任务编号 | T0 |
| 目标 | 建立 P0 的执行控制规则，并把 69 个未匹配市场清单升级为可审核、可追踪的工作清单 |
| 输入 | `P0_执行计划.md`、`unmatched_markets.csv`、既有映射审计结果 |
| 输出 | 修订后的 `P0_执行计划.md`、升级后的 `unmatched_markets.csv`、新建 `P0_执行日志.md` |
| 修改范围 | 仅限计划、清单结构、初始工作状态和日志；不修改原始价格数据、正式映射表或城市维度 |
| 非目标 | 不确认任何市场最终城市；不扩展 117 城市范围；不执行 T1–T8 数据流水线 |
| 验收标准 | CSV 保持 69 行且 `rows` 合计 93,903；原始四列值不变；新增字段齐全；27 个有猜测项进入 `needs_review`，42 个无猜测项进入 `needs_research`；日志记录实际结果 |
| 回退方式 | 保留原始四列作为不可变来源字段；若新增字段或状态初始化失败，只撤销 T0 新增列，不改原始数据 |
| 状态 | `completed`（2026-09-11 12:53 EDT） |

### A1 执行卡：价格单位核验（执行前记录）

| 项目 | 内容 |
|---|---|
| 任务编号 | A1 |
| 目标 | 确认原始“当日价格”“前一日价格”的币种、计量单位与字段含义，并核对“环比”的计算关系，建立可复查的证据链 |
| 输入 | 本地 `data_scraping/README.md`、采集代码、原始 CSV 字段与样例、数据来源页面或其可验证存档 |
| 输出 | `docs/data_dictionary.md` 中的价格口径与来源说明；`P0_执行日志.md` 中的执行记录 |
| 证据优先级 | 官方字段说明/页面 > 官方接口或页面返回字段 > 本地采集代码及原始表头 > 数值量级交叉检查 |
| 修改范围 | 只新增或修订价格字段的数据字典说明与执行日志；不换算价格、不清洗记录、不修改原始数据 |
| 非目标 | 不推断蔬菜规格，不建立城市聚合，不启动 T1–T8，不以常识猜测代替来源证据 |
| 验收标准 | 明确记录币种、计量单位、“当日价格”“前一日价格”“环比”的字段含义、适用范围、来源位置和核验日期；若无法确认，必须标为 `unconfirmed` 并列出缺失证据 |
| 停止条件 | 官方来源不可访问且本地字段/代码不能唯一确定单位时，停止定论，不进行价格换算 |
| 回退方式 | 本步不修改原始价格；数据字典结论若被新证据推翻，以追加更正日志和更新出处的方式修订 |
| 状态 | `completed`（2026-09-11 13:00 EDT） |

### A2 执行卡：`dim_vegetable` 属性来源与参考表（执行前记录）

| 项目 | 内容 |
|---|---|
| 任务编号 | A2 |
| 目标 | 为 30 种蔬菜建立唯一、可解释、可复核的品类维表，补齐易腐性、耐储性和数据驱动价格档位 |
| 输入 | `dataSource/data_scraping/README.md` 的 30 个商品 ID/中英文名、30 个原始 CSV、公开采后储藏资料 |
| 输出 | `reference/dim_vegetable.csv`、`docs/dim_vegetable_methodology.md`、`P0_执行日志.md` 追加记录 |
| 主键 | `vegetable_id` 唯一；英文代码与原始 CSV 文件名一一对应 |
| 易腐性方法 | `perishability_group = high / medium / low`；依据可食部类型、失水/呼吸敏感性和典型采后行为人工归类，每行必须有一句判定依据 |
| 耐储性方法 | `storability_group = short / medium / long`；依据推荐冷藏期或同类商品的典型储藏窗口归类，资料不足时不得伪造精确天数 |
| 价格档位方法 | 对每种蔬菜全部有效 `observed_price` 计算中位数；再按 30 个产品中位数的三分位数切分为 `low / medium / high`，阈值和边界写入方法说明 |
| 修改范围 | 新增品类参考表和方法说明；不修改原始 CSV，不改变价格单位，不用于筛除任何产品 |
| 非目标 | 不建立营养、季节、替代关系或采购评分；不把本表用于预测模型训练；不启动 T1–T8 |
| 验收标准 | 恰好 30 行；ID、英文代码、中文名唯一且非空；三个分类字段完整；价格统计来自 8,682,381 行；人工分类均有依据与来源；CSV 可解析且字段结构一致 |
| 回退方式 | 删除 A2 新增文件即可回退；不影响原始数据和 A1 数据字典 |
| 状态 | `completed`（2026-09-11 13:04 EDT） |

### A3 执行卡：P2 核心蔬菜范围锁定（执行前记录）

| 项目 | 内容 |
|---|---|
| 任务编号 | A3 |
| 目标 | 从 `SDvegetableList.csv` 的 18 个候选中锁定 P2 的 10 个核心蔬菜，并形成可复现、与求职展示目标一致的选择依据 |
| 输入 | `dataSource/otherData/SDvegetableList.csv`、`reference/dim_vegetable.csv`、18 个候选原始 CSV、GUIDEBOOK 当前范围 |
| 输出 | `config/p2_scope.yaml`、`docs/p2_scope_selection.md`、GUIDEBOOK §8 的 10 个英文名/中文名/商品 ID、执行日志追加记录 |
| 数据门槛 | 每个入选产品必须有原始文件、唯一商品 ID、足够的日期/市场覆盖和可接受的正价格率；具体实测值写入选择说明 |
| 组合约束 | 在满足数据门槛后，覆盖叶菜、茄果/瓜类、根茎/块茎和鳞茎等不同采后与定价行为；避免 10 个产品全部集中在同一类 |
| 延续性 | 优先保留原项目已使用且计划中推荐的前 10 个，但必须经过数据门槛复核；若不满足门槛再替换 |
| 修改范围 | 只锁定 P2 分析范围并更新规格与配置；不删除其余 20 个产品，不改变 P0 全量数据层范围 |
| 非目标 | 不训练模型、不计算正式 Tier、不进行城市聚合、不声称 10 个产品代表全部蔬菜 |
| 验收标准 | 恰好 10 个唯一商品 ID；全部属于 18 个候选且存在原始文件；GUIDEBOOK、配置和选择说明三处名单完全一致；选择依据含实测数据和局限 |
| 回退方式 | 恢复 GUIDEBOOK 原范围段并删除 A3 新增配置/说明；不影响 A1–A2 或原始数据 |
| 状态 | `completed`（2026-09-11 13:07 EDT） |

### A4 执行卡：基准 WAPE 诊断固化（执行前记录）

| 项目 | 内容 |
|---|---|
| 任务编号 | A4 |
| 目标 | 将现有 last-value WAPE 诊断固化为参数明确、可重复运行的脚本和版本化结果，为 P2 正式 rolling backtest 提供基准登记 |
| 输入 | `config/p2_scope.yaml`、10 个核心产品原始 CSV、现有市场到城市映射、既有可实施性评审中的诊断数字 |
| 输出 | `src/benchmarks/baseline_wape.py`、`config/baseline_wape.yaml`、`baselines/v0_diagnostic.json`、`docs/baseline_wape_methodology.md`、执行日志追加记录 |
| 诊断模型 | `last_value`；预测值只使用预测原点及以前可见的数据，不使用未来价格 |
| 城市聚合 | 仅使用已有明确映射的市场记录；正价格按城市×蔬菜×日取中位数；未映射市场不进入城市层 |
| 默认门槛 | 产品×城市序列至少 2,000 个有效日期；预测周期 7、14、28 天；缺失处理、评估区间和预测原点规则写入配置，不在代码中隐含 |
| 结果口径 | WAPE 分产品、分预测周期报告，同时记录城市数、预测数、实际值绝对和；不得只保留一个总体平均数 |
| 修改范围 | 新增诊断脚本、配置、结果和方法说明；不修改原始数据、市场映射或 P2 模型范围 |
| 非目标 | 不训练机器学习模型；不将 A4 诊断称为正式 rolling-origin 回测；不据此淘汰产品 |
| 验收标准 | 一条命令可重建 JSON；输入和参数写入结果元数据；10 个产品×3 个周期均有结果；同一环境连续复跑结果一致；与既有诊断数字的差异可解释 |
| 回退方式 | 删除 A4 新增脚本、配置、结果和说明即可回退，不影响 A1–A3 或原始数据 |
| 状态 | `completed`（2026-09-11 13:13 EDT） |

执行中范围补充（2026-09-11 13:11 EDT）：首轮结果已覆盖 10 个核心产品。为避免 GUIDEBOOK 继续保留旧的三产品近似表，A4 允许同步更新 GUIDEBOOK“已知基准与成功标准”中的表格和版本说明；不改动预测验收门槛。

### T1 执行卡：Bronze 摄取层（执行前记录）

| 项目 | 内容 |
|---|---|
| 任务编号 | T1 |
| 目标 | 将 30 个原始 CSV 无损、可追溯地标准化为一个类型明确的 Bronze Parquet，并生成输入与输出清单 |
| 输入 | `dataSource/rawData/*.csv`、`reference/dim_vegetable.csv`、A1 数据字典 |
| 输出 | `src/ingest/build_bronze.py`、`config/paths.yaml`、`data/bronze/fact_market_price_raw.parquet`、对应 manifest、执行日志追加记录 |
| 文件顺序 | 按 `dim_vegetable.csv` 中的 30 个产品顺序摄取；每个英文代码必须存在唯一同名 CSV |
| 追溯键 | `source_file + source_file_row_number` 唯一；同时保留来源 `idx`、原始省份、原始市场名和原始价格字符串 |
| 标准字段 | 增加商品 ID/中英文名；日期保存为 Parquet `date32`；当日/前一日价格与环比保存为 `float64`；原始日期和价格字符串仍保留，避免物理数值类型造成不可逆信息损失 |
| 数据原则 | Bronze 不做市场映射、不删除零值、不裁决异常、不填补缺失、不修改任何原始 CSV |
| 存储规则 | 单一 Parquet，Zstandard 压缩；输出路径来自配置并相对项目目录解析；manifest 保存每个输入文件行数与 SHA-256、输出行数与 SHA-256、模式和日期范围 |
| 验收标准 | 总行数恰好 8,682,381；30 个文件逐一行数守恒；追溯键唯一；日期和三个数值字段解析失败数为 0；来源哈希完整；输出可重新读取且统计与 manifest 一致 |
| 失败处理 | 任一文件字段不符、解析失败、行数不守恒或键重复时不覆盖正式输出；先写临时文件，全部验收后再原子替换 |
| 非目标 | 不做市场映射、质量过滤、城市聚合、Tier 分层或模型特征 |
| 回退方式 | 删除 T1 新增脚本、配置、Bronze 输出和 manifest；原始 CSV 始终不变 |
| 状态 | `completed`（2026-09-11 13:18 EDT） |

执行中范围补充（2026-09-11 13:17 EDT）：本机仅系统 Python 提供 Parquet 依赖。为使 T1 运行条件显式化，允许新增 `requirements.txt`，登记本次验收使用的 Pandas、NumPy 与 PyArrow 版本；不在本步安装或升级软件。

### T2 执行卡：市场映射与 Silver 连接（执行前记录）

| 项目 | 内容 |
|---|---|
| 任务编号 | T2 |
| 目标 | 为 Bronze 中 269 个唯一原始市场名称建立版本化映射状态，并生成逐行带映射结果的 Silver Parquet |
| 输入 | Bronze Parquet、200 行旧 `market_Clean.csv`、`unmatched_markets.csv`、现有 117 城市集合及可验证的市场归属证据 |
| 输出 | `reference/market_mapping.csv`、`reference/market_mapping_CHANGELOG.md`、更新后的 `unmatched_markets.csv`、`config/paths.yaml` 的 Silver 路径、`src/mapping/build_market_mapping.py`、`data/silver/fact_market_price_mapped.parquet` 及 manifest、执行日志追加记录 |
| 名称规范化 | 保存原始名称；另生成 NFKC、统一括号和去空格的 `standardized_name`，以及仅用于候选匹配的后缀简化键；不得用简化键覆盖原始名称 |
| 旧映射 | 200 个旧映射市场保留原 `market_ID`、城市、省份和经纬度，来源标记 `legacy_mapping` |
| 27 个有猜测项 | `city_guess` 只作为候选；名称明确包含城市且省份一致时可用 `explicit_city_in_name` 证据复核，乱码项必须先恢复来源名称 |
| 42 个无猜测项 | 先研究企业/市场实际所在地；确认属于现有 117 城市时映射，确认属于范围外城市时标记 `unmapped_new_city`，证据不足时保留 `unmapped_unknown` |
| 状态枚举 | `mapped_existing_city`、`unmapped_new_city`、`unmapped_unknown`；研究中状态只存在工作清单，不进入最终映射版本 |
| 坐标原则 | 旧映射保留市场坐标；新增市场没有可靠市场级坐标时保持为空，不用城市中心点冒充市场坐标 |
| Silver 规则 | 以 `raw_market_name` 做 many-to-one 连接；全部 8,682,381 行保留，每行 `mapping_status` 非空；只有 `mapped_existing_city` 可供后续城市聚合 |
| 审核留痕 | 每个原 69 项必须填写行动、最终状态、证据来源、审核主体、审核时间和备注；AI 辅助审核明确标为 `codex_reviewed`，不得写成人工批准 |
| 验收标准 | 映射表恰好 269 个唯一原始名称；200 个旧映射值不被改写；69 项全部完成分类或明确未知；Silver 行数 8,682,381；映射状态覆盖 100%；输出可按来源行追溯；manifest 与输出一致 |
| 失败处理 | 任一名称重复、连接扩行、旧映射漂移或证据缺失时不覆盖正式输出；临时文件验收后再原子替换 |
| 非目标 | 不裁决原始省份冲突、不补造市场坐标、不扩展正式 117 城市维度、不进行质量过滤或城市聚合 |
| 回退方式 | 恢复 T0 工作清单并删除 T2 新增映射、Silver、代码和 manifest；Bronze 与原始 CSV 不变 |
| 状态 | `completed`（2026-09-11 13:27 EDT） |

### T3 执行卡：省份冲突审计（执行前记录）

| 项目 | 内容 |
|---|---|
| 任务编号 | T3 |
| 目标 | 对 Silver 中原始省份与正式映射省份不一致的记录建立可追踪清单，区分行政/报送口径差异、来源标签错误与需要重审的映射 |
| 输入 | `data/silver/fact_market_price_mapped.parquet`、`reference/market_mapping.csv`、T2 映射证据与原始日期/市场/商品字段 |
| 输出 | `src/quality/audit_province_conflicts.py`、`docs/province_conflicts.csv`、`docs/province_conflict_methodology.md`、`reference/market_mapping_CHANGELOG.md` 规则说明、执行日志追加记录 |
| 冲突定义 | 仅对 `mapped_existing_city` 记录比较 `raw_province` 与 `market_province`；范围外城市因没有正式省份，不计为冲突，也不借此改写原始值 |
| 汇总粒度 | 每个 `raw_market_name × raw_province × market_province` 一行，并报告记录数、商品数、首末日期、占全部 Silver 记录比例与占该市场记录比例 |
| 裁决枚举 | `reporting_scope_difference`、`source_province_error`、`mapping_review_required`、`unresolved`；每行必须有理由和证据，不用频率高低代替事实依据 |
| 数据原则 | Bronze/Silver 的原始省份保持不可变；T3 只加审计结论，不把映射省份回写到原始字段，不将冲突记录删除出事实表 |
| 验收标准 | 冲突 CSV 行数唯一；`conflict_rows` 合计与 Silver 全量条件计数一致；每行裁决、理由、证据和审核状态非空；无 `mapping_review_required` 时需明确说明依据；输出可重复生成 |
| 失败处理 | 任一冲突组合丢失、计数不守恒或依据不足时不覆盖正式 CSV；无法裁决的项目保留 `unresolved`，不得强行归因 |
| 非目标 | 不修改市场映射、不改变省级行政区划、不建立 T4 价格质量字段、不聚合城市价格 |
| 回退方式 | 删除 T3 新增脚本、冲突清单和方法说明，并回退 changelog 的 T3 追加段；Silver 与映射表不变 |
| 状态 | `completed`（2026-09-11 13:31 EDT） |

### T3R 执行卡：张家界映射版本化纠错（执行前记录）

| 项目 | 内容 |
|---|---|
| 任务编号 | T3R |
| 目标 | 修正 T3 确认的两条旧映射省份错误，消除会污染省级分析的 `mapping_review_required`，并形成可回滚的新映射版本 |
| 输入 | `reference/market_mapping.csv` v0.1、`docs/province_conflicts.csv` v0.1、湖南省政府证据、Silver v0.1 |
| 输出 | 归档 `docs/province_conflicts_v0.1.csv`；升级后的 `reference/market_mapping.csv` v0.2、Silver 与 manifest；重建的 `docs/province_conflicts.csv` v0.2；changelog 和执行日志 |
| 唯一业务改动 | `mk54` 与 `mk173` 的 `canonical_province`、`identified_province` 从河北省改为湖南省，并更新两行的来源、证据与审核元数据；市场 ID、城市、坐标和原始名称不变 |
| 版本规则 | 全表 `mapping_version` 从 v0.1 升为 v0.2；Silver 记录新的映射版本。该全表版本字段变化不视为额外业务改动 |
| 审计规则 | 更正后重新运行 T3 脚本；预期只剩 6 个兵团报送口径组合、107,948 条冲突，且 `mapping_review_required = 0` |
| 数据原则 | 不修改 Bronze，不覆盖 `raw_province`，不更改 23 个范围外城市状态，不补造经纬度 |
| 验收标准 | 除两个指定市场与全表版本字段外映射内容不变；Silver 行数仍为 8,682,381；确认映射率不变；冲突审计与 Silver 条件计数一致；重建哈希稳定 |
| 失败处理 | 任何非预期映射字段漂移、行数变化或审计未清除两条问题时，不接受 v0.2，保留 v0.1 归档用于回退 |
| 非目标 | 不重新审核其余 267 个市场，不裁决价格质量，不进行城市聚合 |
| 回退方式 | 用 `docs/province_conflicts_v0.1.csv` 与日志定位旧审计；恢复 v0.1 映射与配置后重建 Silver。原始 Bronze 始终不变 |
| 状态 | `completed`（2026-09-11 13:36 EDT） |

### T4 执行卡：行级质量字段（执行前记录）

| 项目 | 内容 |
|---|---|
| 任务编号 | T4 |
| 目标 | 在不删除或改写报价的前提下，为每条市场价格记录增加可解释、可复现、避免未来信息泄漏的质量标记 |
| 输入 | 映射 v0.2 的 `data/silver/fact_market_price_mapped.parquet`、A1 字段一致性结果、GUIDEBOOK 的价格黏性限制 |
| 输出 | `config/quality.yaml`、`config/paths.yaml` 的质量表路径、`src/quality/build_market_price_quality.py`、`data/silver/fact_market_price_quality.parquet` 及 manifest、执行日志追加记录 |
| 有效价格 | `price_valid_flag = observed_price > 0 且有限`；无效原因写入 `invalid_price_reason`，原始非正价格仍保留 |
| 环比一致性 | 当前价和前价均为正时，以 A1 已验证的公式复算，绝对误差不超过 0.011 个百分点记为一致；分母无效时结果为空 |
| 重复标记 | 同一 `market_id × vegetable_id × date` 出现多行时全部标为 `duplicate_market_date_flag`；T4 不擅自去重 |
| 零变动与 stale | 市场×蔬菜内按日期和来源行号排序；当前/前价相等且与该序列上一条记录相隔 1 天才累计 `zero_change_run_length`，否则重置；连续至少 7 天标记 `stale_quote_flag` |
| 最近有效报价 | 有效当前价记 0；无效记录记距同一市场×蔬菜最近一个历史有效报价的日数；此前从未有效则为空。该字段不填补缺失日期 |
| 异常值 | 在 `log(observed_price)` 上，仅使用同序列当前行之前最多 90 条报告，至少 30 个有效历史值；以滚动中位数和 IQR 估计稳健尺度，超过 6 倍稳健尺度标记；IQR 为 0 时还须至少偏离 50% 才标记 |
| 运行顺序 | 按 30 个产品依次处理并保持原 Silver 行序；质量字段计算后恢复源顺序，临时输出验收后原子替换 |
| 验收标准 | 行数仍为 8,682,381；输入 29 个字段全量逐值不变；质量字段类型和空值规则符合契约；非正价格计数与 Bronze 一致；异常算法不读取当前行之后的数据；manifest 与输出一致；重复重建哈希稳定 |
| 失败处理 | 任一行数、顺序或输入字段漂移时不覆盖正式质量表；统计规则异常时保留已通过的 mapped Silver |
| 非目标 | 不删除异常/重复/stale 记录，不补值，不计算城市日覆盖、来源可靠性或 Tier，不进行城市聚合 |
| 回退方式 | 删除 T4 新增配置、脚本、质量 Parquet 和 manifest；映射 v0.2 Silver 保持不变 |
| 状态 | `completed`（2026-09-11 17:59 EDT） |

### T5 执行卡：市场事实发布与城市日聚合（执行前记录）

| 项目 | 内容 |
|---|---|
| 任务编号 | T5 |
| 目标 | 发布可供追溯的市场级分析事实表，并按城市×蔬菜×日生成稳健价格、来源规模和可解释可靠性指标 |
| 输入 | `data/silver/fact_market_price_quality.parquet`、`reference/market_mapping.csv`、`reference/dim_vegetable.csv`、`config/aggregation.yaml` |
| 输出 | `reference/dim_city.csv`、`config/paths.yaml` 的维表与 Gold 路径、`config/aggregation.yaml`、`src/reference/build_dim_city_artifact.mjs`、`src/aggregate/build_city_price.py`、`data/gold/fact_market_price.parquet`、`data/gold/fact_city_price.parquet` 及两个 manifest、执行日志追加记录 |
| 城市维度 | 从映射 v0.2 的 `mapped_existing_city` 去重生成恰好 117 个 `city_id`；按省份、城市的 UTF-8 稳定顺序编号为 `city_001`…`city_117`，同一城市只允许一个正式省份，后续事实表同时保留 ID、城市名和省份 |
| 市场事实发布 | 将质量 Silver 的全部 8,682,381 行和 39 列原样发布为市场级 Gold；范围外城市、无效价和质量候选异常继续可见，不把发布动作伪装成清洗 |
| 城市层准入 | 仅使用 `mapping_status = mapped_existing_city`、正式城市/省份非空且 `price_valid_flag = true` 的行；无效价格仍只留在市场事实表 |
| 重复处理 | 先按 `market_id × vegetable_id × date` 取有效价格中位数形成一个市场日，避免重复来源行给同一市场额外权重；同时保留原始有效行数，并把来源行数大于 1 的市场日标为重复 |
| 城市价格 | 对城市日内的市场日价格计算中位数，作为 `analysis_price`；另计算 10% 对称修剪均值，准确删除两端各 `floor(n × 0.10)` 个市场，`n < 10` 时等于普通均值 |
| 质量份额 | `stale_quote_share`、`outlier_share` 分别为城市日内含相应候选标记的市场日占比；`duplicate_market_share` 为含重复来源行的市场日占比；不据此删除有效报价 |
| 可靠性评分 | `0.40×(1-stale_share) + 0.25×(1-outlier_share) + 0.10×(1-duplicate_share) + 0.10×平均映射置信分 + 0.15×min(市场数/2,1)`；映射置信分 high/medium/low/其他分别为 1/0.75/0.5/0 |
| 质量等级 | 可靠性分数 `≥0.85` 为 `good`，`≥0.65` 为 `caution`，否则为 `poor`；市场数只作为评分的 15%，不把单市场城市统一剔除 |
| 时间骨架 | T5 只输出至少有一个有效市场报价的观测城市日，不补齐自然日、不前向填充；完整日历覆盖率、最长缺口和 Tier 在 T6 单独计算 |
| 运行顺序 | 按 30 个蔬菜依次处理；每个产品先做市场日再做城市日，最后按蔬菜维度顺序、城市、日期稳定排序；临时输出通过验收后原子替换 |
| 验收标准 | 城市维表恰好 117 行、ID 和城市唯一且省份无冲突；市场事实与质量 Silver 行数、列、逐值内容和哈希语义一致；城市日主键唯一；全部分析价格有限且大于 0；报告市场数至少 1 且等于市场日计数；份额和可靠性在 [0,1]；未映射/范围外城市为 0；城市价格可由输入样本重算；两个 manifest 与文件一致；重复重建哈希稳定 |
| 失败处理 | 任一市场事实漂移、城市日主键重复、聚合计数不守恒、越界分数或重建不稳定时不接受正式 Gold，保留 T4 质量 Silver |
| 非目标 | 不补齐缺失日期，不做 Tier 分层，不把 outlier/stale 当作错误删掉，不做预测、预警、采购排序或价格冲击传播 |
| 回退方式 | 删除 T5 新增城市维表、脚本、Gold 文件、manifest 和新增配置项；T4 质量 Silver 及此前产物保持不变 |
| 状态 | `completed`（2026-09-11 22:46 EDT） |

### T6 执行卡：覆盖率与 Tier 分层（执行前记录）

| 项目 | 内容 |
|---|---|
| 任务编号 | T6 |
| 目标 | 为全部 30×117 个蔬菜×城市组合生成完整、可解释、可复现的历史覆盖指标和 Tier A/B/C 标签 |
| 输入 | `data/gold/fact_city_price.parquet`、`reference/dim_vegetable.csv`、`reference/dim_city.csv`、T5 manifest |
| 输出 | `config/tiering.yaml`、`config/paths.yaml` 的 Tier 路径、`src/quality/build_coverage_tiers.py`、`data/gold/coverage_tiers.parquet` 及 manifest、`docs/coverage_tier_summary.md`、执行日志追加记录 |
| 评估宇宙 | 对 30 个蔬菜与 117 个正式城市做笛卡尔积，恰好 3,510 行；没有任何有效城市日的组合也必须保留并进入 Tier C |
| 日历分母 | 使用整个城市事实表的最早至最晚有效日期，预期 2014-02-02 至 2022-06-22，共 3,063 个自然日；全局覆盖率为有效城市日数÷3,063 |
| 缺口定义 | 分别报告首个有效日前的 leading gap、最后有效日到数据截止日的 trailing gap、首末有效日之间的最长 internal gap，以及三者最大值；不把缺失日补成价格 |
| 历史质量 | 对有效城市日计算平均 stale/outlier/重复市场占比、平均可靠性、good/caution/poor 日占比、单市场日占比及平均/最大报告市场数；完全无数据时相应均值保持为空 |
| 覆盖评分 | `0.45×全局覆盖率 + 0.20×活跃跨度覆盖率 + 0.15×近期分 + 0.10×新鲜度分 + 0.05×非异常分 + 0.05×非 poor 分`；近期分为 `max(0,1-trailing_gap/365)`，后三个质量分对无数据组合记 0，评分截断在 [0,1] |
| Tier A 门槛 | 有效日期≥2,500；活跃跨度覆盖率≥80%；最长内部缺口≤60 日；尾部缺口≤365 日；平均 stale 占比≤25%；平均 outlier 占比≤10%；poor 日占比≤20%；全部同时满足 |
| Tier B 门槛 | 未达到 A，但有效日期≥365；活跃跨度覆盖率≥20%；最长内部缺口≤365 日；尾部缺口≤730 日；poor 日占比≤60%；全部同时满足 |
| Tier C 规则 | 其余组合，包括完全无有效城市日者；在 `tier_reason` 中列出未满足的门槛，不把 C 类记录删除 |
| 资格字段 | `forecast_eligible_flag` 仅 Tier A 为真；`monitor_eligible_flag` 对 Tier A/B 为真；两者只表示本历史数据集内的分析资格，不代表 2026 年仍可实时使用 |
| 版本与配置 | 阈值写入独立 `config/tiering.yaml`，规则版本 `coverage_tier_v0.1`；不修改 T4 质量和 T5 聚合参数 |
| 验收标准 | 输出恰好 3,510 行；蔬菜×城市主键唯一且维表全覆盖；Tier 非空且只有 A/B/C；计数和日期范围可由城市事实重算；无数据组合必为 C；资格字段与 Tier 一致；比例和评分在 [0,1] 或按契约为空；产品级 Tier 计数每行合计 117；manifest 和摘要一致；第二次重建哈希稳定 |
| 失败处理 | 行数、维表覆盖、主键、边界、Tier 规则、计数守恒或重建稳定性任一失败时不接受正式 Tier 产物，保留 T5 Gold |
| 非目标 | 不调参追求预定 Tier A 数量，不补价格，不训练模型，不改变城市日可靠性评分，不做实时资格或传播分析 |
| 回退方式 | 删除 T6 新增配置、脚本、Tier Parquet、manifest 和摘要；T5 及此前产物保持不变 |
| 状态 | `completed`（2026-09-11 22:53 EDT） |

### T7 执行卡：数据契约测试（执行前记录）

| 项目 | 内容 |
|---|---|
| 任务编号 | T7 |
| 目标 | 建立一条命令可运行的本地数据契约测试，直接验证 Bronze、Silver、Gold 和 Tier 正式产物，而非仅复述 manifest |
| 输入 | T1–T6 的 Parquet、manifest、`reference/dim_vegetable.csv`、`reference/dim_city.csv`、当前配置 |
| 输出 | `tests/test_data_contracts.py`、`README.md` 的测试命令说明、执行日志追加记录 |
| 运行入口 | 使用 Python 标准库 `unittest`：`python3 -m unittest discover -s tests -v`；不新增 pytest 或其他测试依赖 |
| 文件与哈希 | 检查所有正式 Parquet 存在、manifest 登记哈希等于实际文件；Gold 市场事实必须与 T4 质量 Silver 字节一致 |
| Bronze 契约 | 行数 8,682,381、30 个产品/来源文件、日期边界符合 manifest；逐产品检查 `source_file + source_file_row_number` 唯一 |
| Silver 契约 | mapped 与 quality Silver 行数守恒；映射状态只有三个枚举且恒等式合计等于总行数；正式映射率达到 GUIDEBOOK 的 99.5% 目标 |
| 质量逻辑 | 有效价格等价于正且有限；无效原因非空；stale 必须同时为零变动且连续期≥7；环比不一致数为 0；质量统计与 manifest 一致 |
| 城市 Gold 契约 | 每个产品内 `date × vegetable_id × city_id` 唯一；分析价格=中位数且全部为正有限；报告市场数≥1；质量份额/可靠性在 [0,1]；质量等级与阈值一致；来源记录数合计等于准入 Silver 行数 |
| Tier 契约 | 恰好 3,510 个唯一蔬菜×城市组合；完整覆盖 30×117 维表笛卡尔积；有效日数逐产品逐城市与城市事实重算一致；无数据必为 C；Tier、预测/监控资格、manifest 计数和摘要计数一致 |
| 性能方式 | 大表按 Parquet 批次或产品行组验证，避免一次把 8.68M 行全部载入内存；小型 Tier 表可整体读取 |
| 验收标准 | 测试入口退出码为 0；全部测试 PASS；重复运行结果一致；无跳过项；测试过程不改写正式数据 |
| 失败处理 | 任一测试失败时不标记 T7 完成；先判断是测试错误还是数据契约错误，修复必须先补计划，不以放宽断言掩盖正式产物问题 |
| 非目标 | 不重建数据、不调整 Tier 阈值、不训练模型、不增加 CI 服务或 Git 配置 |
| 回退方式 | 删除 T7 新增测试与 README 测试说明；T1–T6 产物保持不变 |
| 状态 | `completed`（2026-09-11 22:53 EDT） |

### T8 执行卡：文档、单命令重建与 P0 最终验收（执行前记录）

| 项目 | 内容 |
|---|---|
| 任务编号 | T8 |
| 目标 | 把 P0 数据资产变成可理解、可复现、可审计的正式交付，并完成 P0 逐项验收 |
| 输入 | GUIDEBOOK、T0–T7 计划/日志、全部 reference/config/manifest/Parquet、T7 测试 |
| 输出 | `src/pipeline/build_p0.py`、扩充后的 `docs/data_dictionary.md`、新建 `docs/data_quality_report.md`、更新的 `README.md`、GUIDEBOOK/P0 交付状态与执行日志 |
| 单命令重建 | `python3 src/pipeline/build_p0.py` 依次运行 Bronze、映射 Silver、省份冲突审计、质量 Silver、市场/城市 Gold、Tier 和测试；任一步失败即非零退出，不静默继续 |
| 数据字典 | 覆盖 Bronze 16 列、映射新增 13 列、质量新增 10 列、城市 Gold 19 列、Tier 35 列及两个核心维表；写明类型、粒度、单位、空值和业务含义 |
| 七项平台指标 | 报告按记录市场匹配率、有效价格率、重复记录率、城市日度覆盖率、当前省份冲突率、Tier A/B/C 数量、数据更新失败率；无法由静态历史数据计算的更新失败率必须标为不可用，不写 0 |
| 补充风险 | 披露零变动/stale/outlier、单市场城市日、范围外城市、修剪均值未实际修剪、极端价格范围、历史截止日和 2026 年实时不可用性 |
| 方法与边界 | 明确市场事实无损、城市准入与两级中位数、可靠性评分、Tier 绝对门槛，以及无成交量/规格/包装/道路距离/实时数据等不可解决限制 |
| README | 说明项目定位、pricing engine 的互补关系、目录/数据层、快速检查、单命令重建、关键文档和下一阶段；不宣称 P1–P5 已完成 |
| 文档一致性 | GUIDEBOOK 与计划中的配置/目录说明同步为实际实现；交付清单只勾选真实存在且通过测试的 P0 项，不把未完成产品模块标成完成 |
| 最终验收 | 运行单命令全量重建并通过；再次运行 T7 测试；核对正式哈希和行数稳定；搜索过期字段名/旧状态/矛盾计数；P0 计划状态和清单更新为完成 |
| 失败处理 | 重建、测试、文档数字或交付清单任一不一致时不标记 P0 完成；先修复并重跑受影响验收，再写 T8 日志 |
| 非目标 | 不建设 dashboard/API，不训练预测/预警/传播模型，不接入实时数据、CI、Git 或外部部署，不改变 T1–T6 已锁定的业务口径 |
| 回退方式 | 删除 T8 新增编排器和质量报告，恢复文档修改；T1–T7 正式数据产物保持可独立使用 |
| 状态 | `completed`（2026-09-12 00:37 EDT） |

### T5C 执行卡：城市 ID 稳定顺序纠错（T8 验收中发现，执行前记录）

| 项目 | 内容 |
|---|---|
| 任务编号 | T5C |
| 发现原因 | T8 文档与代码一致性检查发现：T5 执行卡规定按“正式省份＋城市”的 UTF-8 稳定顺序编号，但 `build_dim_city_artifact.mjs` 实际只按城市的中文 locale 排序 |
| 目标 | 使 `city_id` 的生成实现严格符合 T5 预先记录的确定性顺序，不改市场映射、价格、质量、聚合或 Tier 规则 |
| 输入 | 映射 v0.2、当前 `reference/dim_city.csv`、T5/T6 构建器和正式 Gold |
| 输出 | 修正后的 `src/reference/build_dim_city_artifact.mjs`、重建的 `reference/dim_city.csv`、城市 Gold、Tier、manifest、摘要、测试与追加日志 |
| 排序规则 | 比较键为 `canonical_province + NUL + canonical_city` 的 UTF-8 字节序；再编号 `city_001`…`city_117`，避免依赖系统 locale |
| 影响边界 | 允许 `city_id` 重新分配，以及所有含 `city_id` 的派生文件哈希变化；城市名、省份、纳入市场数、价格、覆盖指标、Tier 标签和全局计数不得变化 |
| 表格工作流 | 以当前映射重新生成 117 行 CSV，重新导入并检查行列数、唯一性、市场数合计与预览；不手工编辑 ID |
| 验收标准 | 117 个 ID/城市唯一；逐行顺序等于 UTF-8 键排序；纳入市场合计仍为 246；按城市名连接前后城市价格和 Tier 业务值一致；T5/T6 重建成功；T7 两次全绿 |
| 失败处理 | 任一非 ID 业务字段变化时停止接受新版本并保留旧产物哈希用于定位；不得通过修改排序说明掩盖实现偏差 |
| 非目标 | 不改变城市范围、映射证据、可靠性权重、Tier 阈值或历史日志中的旧哈希 |
| 回退方式 | 使用 T5 日志中的旧维表/Gold 哈希定位旧版本；恢复旧生成器后重建。Bronze、mapped Silver、quality Silver 与市场 Gold 不受本纠错影响 |
| 状态 | `completed`（2026-09-11 23:02 EDT） |

### T8L 执行卡：最终日志哈希文本纠错（执行前记录）

| 项目 | 内容 |
|---|---|
| 任务编号 | T8L |
| 发现原因 | T8 日志追加后复读发现“当前正式数据哈希”中 Bronze、mapped Silver、city Gold 三行发生字符串重复或误植；实际文件、manifest 与测试均正确 |
| 目标 | 按追加式日志规则写一条更正记录，明确取代 T8 日志中的三处错误文本，不改写历史日志 |
| 输入 | 六个正式 Parquet、manifest、T8 日志的哈希段 |
| 输出 | `P0_执行日志.md` 追加更正、计划状态恢复为全部完成 |
| 验收标准 | 六个当前 Parquet 实际 SHA-256 与 manifest 6/6 一致；更正记录逐字列出正确值并指明被更正段；不改数据、代码或其他文档 |
| 非目标 | 不重建数据，不修改 T8 旧日志段，不改变任何业务结论 |
| 状态 | `completed`（2026-09-12 00:45 EDT） |

---

## 0. 一个会改变 P0 工作量的实测结论

GUIDEBOOK §3 把"审计并补齐 69 个未匹配市场"列为 P0 的主要产出，按 1–2 周估算。实测这 69 个市场后，结论需要修正：

### 0.1 只有 27 个能靠规则映射到现有城市

用"市场名包含现有 117 个城市名"的规则匹配：

| 分类 | 市场数 | 占未映射记录 |
|---|---:|---:|
| 规则可猜中现有城市 | 27 | 35.8% |
| 猜不中 | 42 | 64.2% |

### 0.2 猜不中的 42 个必须先研究，不能直接判定为新城市

其中确有一批名称直接指向 117 城市清单之外的地区，例如渭南、嘉峪关、金昌、白银、天水、武威、酒泉、庆阳、定西、陇南、临夏、固原、中卫等。

但“市场名不包含现有城市名”不能证明它属于新城市。部分市场可能通过企业名、区县名或历史名称间接对应现有城市。因此这 42 个市场的初始状态统一为 `needs_research`，研究后才能分类为 `mapped_existing_city`、`unmapped_new_city` 或 `unmapped_unknown`。

P0 暂不主动扩展 117 城市维度。确认属于新城市的市场保留在市场层并记录原因；若研究发现它属于现有城市，则在人工证据审核通过后补入正式映射。

### 0.3 但这些市场几乎都不构成可用序列 —— 所以不值得扩

以番茄为例，未匹配市场的观测分布：

| 市场 | 起止 | 该蔬菜行数 |
|---|---|---:|
| 鄢陵县勇强农副产品交易市场 | 2020-06-23 → 2021-12-11 | 400 |
| 包头市友谊蔬菜批发市场 | 2014-03-15 → 2022-06-22 | 98 |
| 新疆九鼎农产品经营管理 | 2014-03-15 → 2021-10-05 | 78 |
| 其余大多数 | 2014-03-15 → 2021-10-05 | **39** |

跨 8 年只有 39 个观测日 —— 这些是**间歇性零星上报**，不是时间序列。69 个市场合计 93,903 行（1.08%），分摊到 30 个蔬菜后，多数市场每个蔬菜只有约 50 天数据，**没有一个能进 Tier A**。

### 0.4 据此修订 P0 的市场映射任务

| 处理 | 对象 | 工作量 |
|---|---|---|
| 规则猜测 + 人工复核 | 27 个有 `city_guess` 的市场；初始状态 `needs_review` | 约 0.5 天 |
| 证据研究与分类 | 42 个无 `city_guess` 的市场；初始状态 `needs_research` | 视证据可得性调整 |
| 确认属于现有城市 | 审核通过后映射为 `mapped_existing_city` | 纳入城市聚合 |
| 确认属于范围外城市 | 标记 `unmapped_new_city`，保留在 `fact_market_price` | 不进城市聚合、不进模型 |
| 城市维度扩展 | **不做**，降级为 Post-MVP 待办并说明理由 | — |

P0 使用两个互不替代的指标：

1. **分类完备率 = 100%**：69 个市场最终都有经审核的状态与证据；
2. **按记录数确认映射率**：继续报告，99.5% 作为争取目标，而不是通过猜测强行满足的硬门槛。

不能为了消除 `unmapped_unknown` 或达到映射率目标而降低证据标准。

配套工作清单已生成：`unmatched_markets.csv`（69 行，含原始名、原始省份、记录数、规则猜测城市）。

---

## 1. 先关闭四项开放项（P0 的前置，约 1 天）

| # | 开放项 | 方法 | 产出 | 阻塞谁 | 状态 |
|---|---|---|---|---|---|
| 1 | 价格单位未确认 | 回商务部来源页面（`data_scraping/README.md` 的链接）核对字段说明与单位；若页面已不可用，改用原始抓取脚本的响应字段名 + 与同期公开价格指数交叉验证 | `docs/data_dictionary.md` 的单位一节，附出处与截图 | 全部金额口径 | `completed` |
| 2 | `dim_vegetable` 三个属性无来源 | 人工编制 30 行参考表：`perishability_group`（易腐/中等/耐储）、`storability_group`、`typical_price_level`（从数据算，非人工）。前两个附判定依据一句话 | `reference/dim_vegetable.csv` + 依据说明，纳入版本控制 | §6 的品类对比研究问题 | `completed` |
| 3 | P2 的"10 种蔬菜"未点名 | 从 `otherData/SDvegetableList.csv`（18 个）中取覆盖最好的 10 个，或直接沿用该 18 个列表的前 10 | 写回 GUIDEBOOK §8，列出英文名 + veg_ID | P2 全部范围与验收 | `completed` |
| 4 | 基准 WAPE 缺可复现口径 | 把诊断脚本固化为 `src/benchmarks/baseline_wape.py`，参数写进配置：城市聚合方式、缺失填补上限、城市筛选阈值、计算区间 | 脚本 + `baselines/v0_diagnostic.json` | P2 的基准登记 | `completed` |

第 3 项建议现在就定：`SDvegetableList.csv` 的前 10 个是 Chinese_Cabbage、Cabbage、Cucumber、Tomato、Potato、Green_Pepper、White_Radish、Celery、Eggplant、Onion —— 都是全国流通量大、城市覆盖高的品类，作为 P2 训练集合理。**需要你确认是否就用这 10 个。**

---

## 2. 仓库骨架

```text
produce-price-intelligence/
├─ README.md
├─ GUIDEBOOK.md                    # 产品规格（已定稿）
├─ pyproject.toml / requirements.txt
├─ .gitignore                      # 排除 rawData/ *.zip *.parquet
├─ config/
│  ├─ paths.yaml                   # 数据根目录，不写绝对路径进代码
│  ├─ quality.yaml                 # 行级质量阈值
│  ├─ aggregation.yaml             # 城市聚合与可靠性评分
│  ├─ tiering.yaml                 # 覆盖评分与 Tier 门槛
│  └─ backtest.yaml                # P2 时间切分、折数（待建）
├─ reference/                      # 受版本控制的人工参考表
│  ├─ dim_vegetable.csv
│  ├─ dim_city.csv
│  ├─ market_mapping.csv           # 版本化市场映射（P0 核心产出）
│  └─ market_mapping_CHANGELOG.md
├─ src/
│  ├─ ingest/      # 读 rawData → 标准化 → fact_market_price
│  ├─ mapping/     # 市场名标准化与映射解析
│  ├─ quality/     # 质量字段、Tier 分层、质量报告
│  ├─ aggregate/   # market → city 聚合
│  ├─ benchmarks/  # 简单基准（前置项 4）
│  └─ pipeline/    # P0 单命令重建
├─ tests/          # 数据契约测试
├─ docs/
│  ├─ data_dictionary.md
│  └─ data_quality_report.md
└─ data/           # gitignored，Parquet 落地
   ├─ bronze/  silver/  gold/
```

**约定**：绝对路径只出现在 `config/paths.yaml`；任何清洗规则不得写在分析 notebook 里。

---

## 3. P0 任务分解

### T0 开工准备
落实本文件“执行管理规则”；升级 `unmatched_markets.csv`；建立追加式 `P0_执行日志.md`。T0 完成前不进入 T1。

### T1 摄取层（bronze）
读 30 个 csv → 统一列名（按 §3 映射表）→ 解析日期与数值 → 写 `bronze/fact_market_price_raw.parquet`，保留 `source_file`、`source_row_index`、原始省份、原始市场名，**不做任何清洗**。

验收：行数 = 8,682,381；可按文件回溯每一行。

### T2 市场映射（reference + silver）
1. 名称标准化：全角/半角、括号、空格、公司后缀
2. 精确匹配现有 200 个 → 3. 对 27 个规则猜测进行人工复核 → 4. 对 42 个无猜测项进行证据研究 → 5. 分类为 `mapped_existing_city`、`unmapped_new_city` 或 `unmapped_unknown`
6. 产出 `reference/market_mapping.csv`，字段：`raw_market_name, standardized_name, market_id, city, province, lon, lat, mapping_status, mapping_confidence, mapping_source, mapping_version, reviewed_by, reviewed_at`

验收：69 个市场的 `review_status = codex_reviewed`，需要人工审批时另设状态，不能把 AI 复核写成人工批准；每条原始记录的 `mapping_status` 非空；`mapped_existing_city + unmapped_new_city + unmapped_unknown = 100%`；映射表可 diff、可回滚；未知项不得以猜测代替证据。

### T3 省份冲突处理
2.23% 的记录原始省份与映射省份不一致。**不自动裁决**：写 `docs/province_conflicts.csv`（市场、原始省份、映射省份、记录数），抽样人工判断是市场迁址还是源数据错误，规则写进 CHANGELOG。

验收：冲突记录 100% 可追踪，裁决规则有书面依据。

### T4 质量字段（silver）
Silver 行级计算：`mapping_status`、`mapping_confidence`、价格有效性、`stale_quote_flag`、`zero_change_run_length`、`days_since_last_valid_quote`、`outlier_flag`。

`number_of_reporting_markets` 与 `source_reliability_score` 在 T5 城市日聚合层计算；`coverage_score` 与 `coverage_tier` 在 T6 的蔬菜×城市序列表计算，避免把不同粒度的字段混入行级事实表。

注意：`stale_quote_flag` 标的是**源数据已结转**的事实，不做任何还原（§4 已定）。

### T5 城市聚合（gold）
`fact_city_price`：按城市×蔬菜×日聚合 median / trimmed mean，附 `number_of_reporting_markets`、`stale_quote_share`、`source_reliability_score`、`data_quality_flag`。

**只用 `mapping_status = mapped_existing_city` 且价格有效的记录聚合**，范围外与无效记录留在 market 层。

### T6 Tier 分层
按 §4「覆盖分层」，对每个**蔬菜×城市**分别定 Tier A/B/C。有效日期≥2500只是 Tier A 的一个必要门槛；完整绝对阈值写进 `config/tiering.yaml`，不硬编码。正式结果各蔬菜 Tier A 为3–44个城市，中位31。

产出 `gold/coverage_tiers.parquet` + 每蔬菜 Tier 计数表。

### T7 数据契约测试
`tests/` 至少覆盖：行数守恒、日期范围、分析价格非负、主键唯一、映射率恒等式、Tier 分层非空、聚合层不含未映射记录。Bronze 原始价格按源数据保留，非正值只在 Silver/Gold 的有效性规则中处理。CI 可选，但测试必须能本地一条命令跑完。

### T8 文档
`docs/data_dictionary.md`（含单位出处、原始→标准映射、全部表字段）
`docs/data_quality_report.md`（§7「数据平台」的 7 项指标 + 价格黏性与离散化的实测数字 + 不可解决限制声明）

---

## 4. 交付清单

- [x] `reference/market_mapping.csv` + CHANGELOG
- [x] `reference/dim_vegetable.csv` + 判定依据
- [x] `data/gold/fact_city_price.parquet`、`fact_market_price.parquet`、`coverage_tiers.parquet`
- [x] `docs/data_dictionary.md`（含**已确认**的价格单位）
- [x] `docs/data_quality_report.md`
- [x] `docs/province_conflicts.csv`
- [x] `src/benchmarks/baseline_wape.py` + `baselines/v0_diagnostic.json`
- [x] `tests/` 全绿
- [x] GUIDEBOOK §8 补上 10 个蔬菜的名单；§7 映射率规则按 §0.4 改写

---

## 5. 修订后的 P0 门槛

GUIDEBOOK §9 原文：「按记录数映射率 ≥99.5%；冲突记录可追踪；产品级城市分层完成」

P0 最终验收口径：

1. **分类完备性**：69 个未匹配市场 100% 完成审核并有证据；`mapped_existing_city + unmapped_new_city + unmapped_unknown = 100%`
2. **确认映射率**：按记录数持续报告，99.5% 为争取目标而非强制硬门槛；`unmapped_unknown = 0` 是研究目标，不得通过无证据归类强行实现
3. **冲突可追踪**：2.23% 省份冲突全部留档，裁决规则书面化
4. **分层完成**：每个蔬菜×城市有 Tier 标签；正式绝对门槛下 Tier A 为3–44个城市/蔬菜、中位31，并保留逐项失败原因
5. **单位已确认**：数据字典写明价格单位及出处，不含推测
6. **可复现**：一条命令从 rawData 重建全部 gold 表，测试全绿

---

## 6. 时间表

| 任务 | 估时 |
|---|---|
| 前置四项开放项 | 1 天 |
| T1 摄取 | 0.5 天 |
| T2 映射 | 1 天 |
| T3 省份冲突 | 0.5 天 |
| T4 质量字段 | 1 天 |
| T5 聚合 | 0.5 天 |
| T6 Tier 分层 | 0.5 天 |
| T7 测试 | 0.5 天 |
| T8 文档 | 1 天 |
| **合计** | **约 6.5 天** |

比 GUIDEBOOK 原估的 1–2 周短，主要来自 §0.4 的市场映射范围收缩。

以上估时是排期参考，不是交付承诺。42 个无猜测市场的研究时间取决于可获得的来源证据；若证据不足，应保留 `unmapped_unknown` 并记录原因，而不是按工期强行归类。

---

## 7. 需要你先定的三件事

1. **P2 的 10 个蔬菜**：是否采用 `SDvegetableList.csv` 的前 10 个（见 §1 第 3 项）？
2. **42 个待研究市场**：P0 默认不扩城市维度；确认属于现有城市的补入映射，确认属于新城市的标记不映射、不进模型，证据不足的保留未知并说明原因。是否采用此规则？
3. **价格单位**：你是否已有来源页面的记录？没有的话我可以先尝试访问商务部页面确认。
