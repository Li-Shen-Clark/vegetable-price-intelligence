# P1 执行计划：Historical Price Monitor

对应 `GUIDEBOOK.md` §5 模块一与 §9 的 P1 阶段。
编制日期：2026-09-12
状态：**P1 v0.1 本地验收完成（2026-09-12 04:52 EDT）；未公开部署**

---

## 1. 执行管理规则

P1 延续 P0 的“先计划、后执行、再留痕”规则：

1. **计划先行**：每项任务执行前，先在本文件写明目标、输入、输出、修改范围、非目标、验收标准与回退方式。
2. **按卡执行**：未进入执行卡的功能不顺带实现；如需扩展范围，先补写计划。
3. **执行后记录**：每项任务完成或中止后，立即在 `P1_执行日志.md` 追加实际动作、变更文件、验证结果、偏差与下一步门槛。
4. **日志只追加**：历史记录不覆盖；如需纠正，以新日志引用原记录。
5. **逐步放行**：上一步未达到验收标准，不进入下一步。
6. **事实与演示分离**：真实聚合指标必须来自 P0 Gold；界面骨架如需临时占位，必须明确标为 demo，并在正式验收前移除。
7. **历史边界常驻**：任何页面都不得暗示实时行情；必须持续展示数据截止日 `2022-06-22`。

---

## 2. P1 产品边界

### 2.1 要解决的问题

为定价、采购和商业分析岗位展示一套可追溯的历史市场情报工作台，使使用者能回答：

- 某种蔬菜在某个城市的价格如何随时间变化？
- 当前所选历史月份相对前期处于什么水平？
- 同期哪些城市价格更高或更低，区域价差多大？
- 这条趋势和排名是否受覆盖不足、单一来源或低可靠性影响？
- 价格是否存在稳定季节性，还是由少数异常阶段主导？

### 2.2 与 pricing engine 的关系

本模块是现有 pricing engine 的**互补情报层**，不是同类执行产品：

- Historical Price Monitor 负责历史基准、市场比较、季节性和数据质量证据；
- pricing engine 负责当前报价、规则、约束和实际定价动作；
- P1 的指标未来可以成为 pricing engine 的参考输入，但 P1 不输出“今日应卖多少钱”，也不执行价格。

### 2.3 数据与功能范围

- 数据范围：2014-02-02 至 2022-06-22，117 个城市、30 种蔬菜。
- 默认分析范围：Tier A/B 城市×蔬菜组合；Tier C 可在质量说明中披露，但不默认进入可比较排名。
- 展示粒度：月度为首版主视图，来自城市日 Gold 的月内中位数；同时保留月内有效天数、来源市场数和可靠性指标，避免把月度平滑误称为日度报价。
- 核心页面：单页分析工作台，不制作营销首页。
- 核心视图：历史趋势、选择月份的城市排名/区域价差、历史季节区间、数据质量说明。
- 首版不包含：实时更新、预测、预警、采购优化、价格冲击传播网络、用户账户、编辑或交易操作。

### 2.4 信息架构与视觉方向

- 第一屏直接呈现筛选器、关键指标和趋势图；不使用大幅宣传式首屏。
- 顶部常驻“Historical / 数据截至 2022-06-22”状态。
- 主色使用深绿与暖白，涨跌采用可访问的红/蓝辅助色，不只依赖颜色传达含义。
- 控件与反馈优先使用项目脚手架内的 shadcn 组件，图表使用其 Recharts 封装，图标使用 Lucide。
- 桌面端优先，同时保证窄屏下筛选、图表和排名可阅读。

### 2.5 数据服务策略

浏览器不直接加载 735 万行城市日事实。P1 建立可复现的 Web mart：

```text
data/gold/fact_city_price.parquet
reference/dim_vegetable.csv
reference/dim_city.csv
data/gold/coverage_tiers.parquet
              ↓ 可复现聚合
web/public/data/metadata.json
web/public/data/products/<vegetable_id>.json
              ↓ 按蔬菜按需加载
Historical Price Monitor
```

每个产品文件只保存月度城市指标；前端一次只读取当前蔬菜文件。产品文件必须包含口径版本、生成时间、来源日期范围和聚合字段说明。

---

## 3. 任务顺序与放行门槛

| 任务 | 主要产出 | 放行门槛 | 状态 |
|---|---|---|---|
| P1-T0 产品与实现契约 | 本计划、日志、明确的范围和验收口径 | 范围、数据边界、架构、非目标均可复查 | `completed` |
| P1-T1 Web mart | 聚合脚本、30 个产品 JSON、metadata、数据测试 | 来源守恒；30/30 产品；所有指标可追溯；体积可接受 | `completed` |
| P1-T2 站点骨架与首个可用切片 | `web/` 站点、主题、筛选、KPI、真实趋势图 | 本地构建成功；默认页使用真实数据；明确历史截止日 | `completed` |
| P1-T3 排名、价差与季节区间 | 城市排名、区域价差、季节带、联动状态 | 所有视图响应相同筛选，计算口径一致 | `completed` |
| P1-T4 质量解释与产品状态 | 质量面板、空/错/加载状态、方法提示 | 风险可见；Tier C/低质量不被静默当作可靠信号 | `completed` |
| P1-T5 数据故事与验证 | 至少 3 个可复现故事、自动检查、窄屏检查 | 故事数字可由 mart 重算；无实时误导；构建与检查通过 | `completed` |
| P1-T6 文档与交付 | README/GUIDEBOOK 同步、运行说明、可选发布 | P1 清单完成；本地可复现；发布动作另行留痕 | `completed` |

---

## 4. 执行卡

### P1-T0 执行卡：产品与实现契约（执行前记录）

| 项目 | 内容 |
|---|---|
| 任务编号 | P1-T0 |
| 目标 | 在写数据聚合或界面代码前，锁定 P1 的用户问题、数据边界、页面结构、技术路线和验收顺序 |
| 输入 | `GUIDEBOOK.md`、`README.md`、P0 Gold/维表/Tier、Sites 建站规范 |
| 输出 | `P1_执行计划.md`、`P1_执行日志.md` |
| 修改范围 | 仅新增 P1 计划与日志；不修改 P0 数据、P0 文档或站点代码 |
| 非目标 | 不生成 Web mart，不搭建前端，不部署，不改写 P0 结论 |
| 验收标准 | 计划明确历史截止日、默认 Tier、月度口径、浏览器数据策略、任务依赖、每步验收与回退方式；日志记录实际结果 |
| 回退方式 | 删除 P1 两份控制文档即可，不影响 P0 |
| 状态 | `completed`（2026-09-12 01:34 EDT） |

### P1-T1 执行卡：Web mart（待 P1-T0 完成后启动）

| 项目 | 内容 |
|---|---|
| 任务编号 | P1-T1 |
| 目标 | 将 P0 城市日事实聚合为浏览器可按产品加载的月度监控数据，同时保留覆盖与可靠性证据 |
| 输入 | `data/gold/fact_city_price.parquet`、`data/gold/coverage_tiers.parquet`、`reference/dim_city.csv`、`reference/dim_vegetable.csv` |
| 输出 | `src/monitor/build_monitor_data.py`、`web/public/data/metadata.json`、`web/public/data/products/*.json`、P1 数据契约测试 |
| 聚合主键 | `vegetable_id × city_id × month` 唯一；月份使用自然月首日标识 |
| 核心指标 | 月度中位价格、25/75 分位、有效天数、最早/最晚观测日、月均可靠性、good/caution/poor 天数、来源市场数中位数、Tier |
| 排名准入 | 默认只比较 Tier A/B 且所选月有效天数达到配置门槛的城市；门槛必须进入 metadata，不在前端暗藏 |
| 文件策略 | 30 个产品各一个 JSON；metadata 单独保存维表、可选项、默认项、数据日期与口径版本；前端不读取 Parquet |
| 修改范围 | 新增监控数据生成器、Web mart 与相应测试；可补充专用配置；不修改 P0 正式 Parquet |
| 非目标 | 不插值、不预测、不把月度中位数伪装为最新实时报价、不生成 UI |
| 验收标准 | 30/30 产品成功；主键唯一；数值与 Gold 抽样重算一致；Tier 和城市映射完整；JSON 可解析；记录数与体积写入日志；连续两次重建内容稳定（生成时间字段除外需固定或移出内容） |
| 失败处理 | 先写临时目录，全部验证后替换正式 mart；任何产品失败时不接受部分输出 |
| 回退方式 | 删除 P1-T1 新增脚本、配置、测试和 `web/public/data`；P0 不受影响 |
| 状态 | `completed`（2026-09-12 01:38 EDT） |

### P1-T2 执行卡：站点骨架与首个可用切片（待 P1-T1 完成后启动）

| 项目 | 内容 |
|---|---|
| 任务编号 | P1-T2 |
| 目标 | 建立单页分析工作台，并用真实 mart 完成“选择蔬菜与城市 → 查看 KPI 和月度趋势”的首个闭环 |
| 输入 | P1-T1 Web mart、Sites 脚手架、shadcn 控件与图表组件 |
| 输出 | `web/` 前端项目、主题与布局、筛选器、KPI、趋势图、历史截止日状态、首个本地预览 |
| 默认状态 | 使用 metadata 中可用且覆盖良好的默认蔬菜/城市；不得硬编码一个不存在或 Tier C 的组合 |
| 首屏 | 产品/城市筛选、历史状态、所选月份价格、环比、城市排名、可靠性、主趋势图 |
| 修改范围 | 只实现上述闭环和基础响应式布局；不提前加入传播网络、预测或采购模块 |
| 非目标 | 不部署；不制作营销页；不使用虚构数字；不做浏览器自动化视觉 QA，除非用户另行要求 |
| 验收标准 | 依赖安装与生产构建成功；本地请求返回成功；真实数据可切换；首屏明确“月度中位数”和截止日；加载/失败状态可理解 |
| 回退方式 | 删除 `web/` 中除 `public/data` 外的站点骨架即可；Web mart 可独立保留 |
| 状态 | `completed`（2026-09-12 04:02 EDT） |

### P1-T3 执行卡：排名、区域价差与季节区间（待 P1-T2 完成后启动）

| 项目 | 内容 |
|---|---|
| 任务编号 | P1-T3 |
| 目标 | 补齐跨城市比较和历史季节性，使趋势不仅“能看”，还能解释相对位置和季节范围 |
| 输入 | P1-T1 mart、P1-T2 工作台状态 |
| 输出 | 所选月城市排名、最高/最低与分位价差、区域分布摘要、按历月计算的历史季节区间 |
| 联动规则 | 蔬菜、城市、月份使用单一状态源；排名点击可切换城市；所有数字使用同一准入口径 |
| 季节口径 | 对所选城市×蔬菜按历月汇总跨年中位数与四分位区间，明确样本年数，不使用未来预测表述 |
| 价差口径 | 只在同一蔬菜、同一月份、满足排名准入的城市间比较；同时显示可比城市数 |
| 非目标 | 不做地图路线或运输成本；不推断因果；不构建传播网络 |
| 验收标准 | 筛选联动一致；空月/少样本不报伪排名；抽样数字可由 JSON 重算；键盘可操作排名行 |
| 回退方式 | 移除 T3 新增组件与状态，不影响 P1-T2 趋势闭环 |
| 状态 | `completed`（2026-09-12 04:43 EDT） |

### P1-T4 执行卡：质量解释与产品状态（待 P1-T3 完成后启动）

| 项目 | 内容 |
|---|---|
| 任务编号 | P1-T4 |
| 目标 | 让使用者能判断信号可信度，并覆盖加载、无数据、低覆盖和错误状态 |
| 输入 | mart 中的 Tier、可靠性、质量日数与来源市场数，P0 质量方法 |
| 输出 | 质量面板、指标解释、状态图例、错误/空/加载界面、Tier C 限制提示 |
| 质量原则 | 质量状态与价格同屏；颜色之外使用文字/图标；poor 数据不静默进入“正常”叙述 |
| 非目标 | 不重新定义 P0 质量规则；不允许用户在 P1 修改数据 |
| 验收标准 | 每个主视图都能追溯质量；网络或文件错误不会显示过期假数据；无数据组合有下一步提示；文案无实时暗示 |
| 回退方式 | 回退 T4 展示组件，不修改 mart 与 P0 质量字段 |
| 状态 | `completed`（2026-09-12 04:46 EDT） |

### P1-T5 执行卡：数据故事与验证（待 P1-T4 完成后启动）

| 项目 | 内容 |
|---|---|
| 任务编号 | P1-T5 |
| 目标 | 形成至少 3 个可复现、对求职展示有用的数据故事，并完成数据、构建和窄屏验收 |
| 输入 | 完整 P1 工作台与 mart |
| 输出 | `docs/p1_data_stories.md`、自动化数据/界面逻辑检查、验证记录 |
| 故事要求 | 至少覆盖趋势/季节、跨城市价差、质量风险三类；每个故事列明筛选条件、指标口径、观察和不能推出的结论 |
| 验证范围 | mart 契约、筛选与排名纯函数、构建、轻量本地请求、关键窄屏布局；不以截图替代数据断言 |
| 非目标 | 不夸大因果或实时价值；不借 P1 故事提前宣称 P2/P5 结果 |
| 验收标准 | 3 个故事均可重算；自动检查通过；桌面和窄屏无关键内容溢出；错误与空状态可触发验证 |
| 回退方式 | 删除故事文档和新增测试，不影响已完成工作台功能 |
| 状态 | `completed`（2026-09-12 04:50 EDT） |

### P1-T6 执行卡：文档与交付（待 P1-T5 完成后启动）

| 项目 | 内容 |
|---|---|
| 任务编号 | P1-T6 |
| 目标 | 将 P1 的运行、数据口径、产品边界和实际完成状态写回项目文档，形成可复现交付 |
| 输入 | P1-T1 至 T5 的正式产物与验证结果 |
| 输出 | 更新后的 `README.md`、`GUIDEBOOK.md`、P1 最终清单与日志；如执行发布，则另记发布地址和验证 |
| 发布规则 | 本地交付与公开发布分开验收；任何外部发布只在功能完成后执行，并记录平台、时间、版本和失败回退 |
| 非目标 | 不把 P2–P6 标记为完成；不初始化 Git；不承诺实时更新 |
| 验收标准 | 一条清晰命令可重建 mart；一条命令可启动站点；文档与界面口径一致；P1 清单真实；所有已知限制可见 |
| 回退方式 | 文档按新增段落回退；站点和数据产物不因文档回退而改变 |
| 状态 | `completed`（2026-09-12 04:52 EDT；本地交付，未公开部署） |

---

## 5. P1 完成定义

只有同时满足以下条件，P1 才能标记完成：

- 30 种蔬菜的月度 Web mart 可由 P0 Gold 一条命令重建；
- 默认 Tier A/B 组合可完成筛选、KPI、趋势、排名、价差、季节区间与质量解释；
- 页面始终明确数据截至 2022-06-22，且所有价格明确为月度历史统计；
- 至少 3 个数据故事可由正式 mart 重算；
- 数据契约、前端逻辑、生产构建和本地请求验证全部通过；
- `README.md`、`GUIDEBOOK.md`、执行计划与追加式日志互相一致；
- 若尚未发布，明确写“本地完成、未公开部署”，不得以本地预览冒充上线。

---

## 6. P1 最终验收（2026-09-12）

- [x] P1-T0 至 P1-T6 均按“先计划、后执行、再日志”完成。
- [x] 30 种蔬菜、117 个城市、285,723 条城市×月份 Web mart 可由一条命令重建。
- [x] Historical Price Monitor 完成筛选、KPI、趋势、排名、价差、季节区间和质量解释。
- [x] 页面常驻历史状态与数据截止日，最后完整月份固定为 2022-05。
- [x] 3 个数据故事由正式 mart 自动生成，连续生成哈希一致。
- [x] P0+P1 共 17 项测试通过，0 失败、0 跳过。
- [x] TypeScript 检查、生产构建、本地页面与 metadata 请求通过。
- [x] `README.md`、`GUIDEBOOK.md`、计划与日志状态一致。
- [x] 当前状态明确为“本地完成、未公开部署”；没有擅自初始化 Git 或执行外部发布。

### 已知非阻断项

- 生产构建提示 Recharts 所在客户端 chunk 超过 500 kB，后续可做延迟加载或代码拆分。
- npm 首次安装报告 11 个依赖审计项，尚未在兼容性评估前执行强制升级。
- 当前未做浏览器截图/交互视觉 QA；已完成响应式源码契约、TypeScript、构建和 HTTP 验证。
- P1 v0.1 未包含日级下钻、同比和滚动波动率，这些是后续 Monitor 增强项。
