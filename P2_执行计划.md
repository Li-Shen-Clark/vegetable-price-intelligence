# P2 执行计划：概率预测与预测区间

> 状态：P2 v0.1 已完成（partial_release）  
> 下一阶段：P3 价格风险预警（尚未开工）  
> 前置条件：P0 数据底座、P1 历史价格监控均已本地验收  
> 产品定位：本模块是现有 pricing engine 的上游决策情报层，提供未来价格分布、风险区间与可信度；不替代报价、调价规则或交易执行。

## 1. 执行管理规则

1. 每个任务开始前，先在本文件写清目标、输入、输出、验收标准、非目标与回滚方式。
2. 实施结束后，再向 `P2_执行日志.md` 追加实际动作、产物、验证结果、偏差与下一步；日志只追加，不改写历史记录。
3. 正式结论只使用 P0 Gold 数据、冻结的市场映射和覆盖分层；旧版诊断结果只作参考，不混入正式评分。
4. 所有模型比较必须使用相同的预测行，避免因缺失值或样本变化制造虚假提升。
5. 2021-01-01 至 2022-06-22 的最终测试集在模型和阈值冻结前不得用于选择模型、特征或超参数。
6. 未通过 Go/No-Go 的模型不得接入默认产品界面；可以保留为研究结果并说明失败原因。
7. 每项输出都要可复现、可测试、可回滚，不以单次 notebook 结果作为正式交付。

## 2. 冻结范围与核心口径

### 2.1 产品范围

P2 MVP 固定使用 `config/p2_scope.yaml` 中的 10 种核心蔬菜：大白菜、圆白菜、黄瓜、西红柿、土豆、青椒、白萝卜、芹菜、茄子、洋葱。范围变更必须先修改计划并记录原因。

### 2.2 市场范围

- Tier A：正式训练、验证和测试池，也是对外成功声明的唯一主样本。
- Tier B：影子评估与覆盖扩展诊断，不参与正式 Go/No-Go 汇总，除非后续通过独立计划升级。
- Tier C：P2 MVP 排除。
- 某个产品—城市组合必须在相应预测期内满足数据资格规则，才进入该期评分。

### 2.3 时间切分

- 训练期：数据起点至 2019-12-31。
- 验证期：2020-01-01 至 2020-12-31，用于模型、特征和阈值选择，并单独标记疫情压力环境。
- 最终测试期：2021-01-01 至 2022-06-22，仅在模型规格冻结后运行一次正式评估。
- 回测采用 expanding window；验证期至少 12 个预测起点，最终测试期至少 12 个预测起点。
- 正式预测跨度：7、14、28 个自然日；7/14 日是 MVP 主门槛，28 日作为中期风险信息与次级门槛。

### 2.4 预测对象

- 主目标：产品—城市未来价格水平，训练时优先使用 `log(price)`，展示时还原到原价格单位。
- 辅助诊断：相对预测起点的价格变化、对数收益和涨跌方向。
- 正式目标日期必须有真实观测，不对目标值插值或前向填充。
- 预测起点优先使用当日真实有效价格；如实验契约允许，最多只可使用前 1 日的有效价格，并必须保留填充标记。

### 2.5 基线与指标

必须同时评估三类简单基线：最近有效价格、7 日周期价格、历史季节中位数。每个产品和跨度取同一评分行上的最佳简单基线作为比较对象。

- 点预测主指标：WAPE。
- 稳健性指标：MAE、sMAPE、产品内 series-level WAPE 改善分布、改善的城市占比。
- 概率预测指标：P10/P50/P90 pinball loss、80% 区间覆盖率、平均区间宽度、WIS。
- 可靠性加权指标仅作诊断，不替代未加权正式指标。

### 2.6 Go/No-Go 原则

- 目标线：相对最佳简单基线的正式测试 WAPE 改善至少 8%。
- 最低可接受线：7 日和 14 日上，至少 6/10 个产品取得正向 WAPE 改善，且产品内超过 50% 的合格城市改善；不得由少数大体量序列单独驱动。
- 风险约束：任何主产品—主跨度不得出现超过 5% 的相对 WAPE 恶化而未被明确降级或回退到基线。
- 概率预测：80% 区间覆盖率目标为 75%–85%，同时报告宽度和 WIS，禁止通过无限加宽区间换取覆盖率。
- 未达到最低线时，产品默认展示最佳基线和“不确定性不足/暂不推荐”状态，不包装为模型成功。

## 3. 任务总览

| 任务 | 内容 | 状态 | 核心产物 |
|---|---|---|---|
| P2-T0 | 预测实验契约与可行性审计 | 已完成 | 实验配置、协议、可行性审计 |
| P2-T1 | 防泄漏建模数据集 | 已完成 | 预测样本表、特征字典、数据契约测试 |
| P2-T2 | 正式基线与滚动回测记分牌 | 已完成 | 基线预测、统一评分器、基线报告 |
| P2-T3 | 确定性点预测模型 | 已完成 | 候选模型、验证集比较、冻结模型规格 |
| P2-T4 | 概率预测与区间校准 | 已完成 | P10/P50/P90、校准器、概率评分报告 |
| P2-T5 | 稳定性、误差分析与 Go/No-Go | 已完成 | 最终测试报告、模型卡、发布清单 |
| P2-T6 | 产品界面集成 | 已完成 | 预测页、风险区间、基线回退与解释 |
| P2-T7 | 文档与本地交付验收 | 已完成 | 使用说明、验证记录、Guidebook 更新 |

## 4. 任务卡

### P2-T0：预测实验契约与可行性审计

**目标**

在训练任何模型前冻结正式实验边界，并用 Gold 数据验证 10 种蔬菜、Tier A 市场、时间切分和预测跨度是否有足够的合格样本。

**输入**

- `data/gold/fact_city_price.parquet`
- `data/gold/coverage_tiers.parquet`
- `config/p2_scope.yaml`
- `config/baseline_wape.yaml`
- `docs/baseline_wape_methodology.md`
- `baselines/v0_diagnostic.json`
- `GUIDEBOOK.md`

**实施内容**

1. 创建机器可读的 `config/p2_experiment.yaml`，冻结数据版本、范围、切分、回测起点、资格规则、基线、指标与门槛。
2. 创建可复现的只读审计程序，检查各产品的 Tier 数量、各时间段覆盖、质量分布、预测起点/目标配对数量以及 7/14/28 日可评估性。
3. 输出 `docs/p2_feasibility_audit.json` 与 `docs/p2_feasibility_audit.md`。
4. 输出 `docs/p2_experiment_protocol.md`，用非代码语言解释正式实验设计、泄漏防护与失败回退。
5. 为配置解析和关键资格规则增加自动化测试。

**验收标准**

- 10 个产品、Tier A 正式池、训练/验证/测试日期、预测跨度和回测起点全部有唯一机器可读定义。
- 每个产品在验证和最终测试期均达到至少 12 个可用回测起点，或在审计中被明确标红并提出降级方案。
- 审计能重跑，并记录输入路径、运行时间、关键计数和所有资格规则。
- 协议明确禁止目标填充、未来特征、跨期拟合统计量以及用最终测试集调参。
- 本任务不训练模型，不发布性能承诺。

**非目标**

- 不选择最终算法。
- 不把旧版全历史 WAPE 当作正式基线。
- 不接入前端页面。
- 不引入天气、节假日等新增外部数据。

**回滚方式**

删除本任务新增的 P2 配置、审计程序与文档即可；不修改 P0/P1 数据和界面。

### P2-T1：防泄漏建模数据集

**目标**

基于冻结实验契约建立产品—城市—预测起点—跨度级建模数据集，保证每个特征在预测时真实可获得。

**计划输出**

- `config/p2_features.yaml`：冻结训练起点、特征窗、as-of 容忍度与目标字段。
- `data/modeling/p2_forecast_mart/train.parquet`：2016-01 至 2019-12 的 48 个月初训练起点。
- `data/modeling/p2_forecast_mart/validation.parquet`：T0 冻结的 2020 年 12 个验证起点。
- `data/modeling/p2_forecast_mart/final_test.parquet`：T0 冻结的 2021-01 至 2022-05 共 17 个测试起点；独立文件保存，后续模型冻结前不得读取其目标做选择。
- `data/modeling/p2_forecast_mart/manifest.json` 与 `docs/p2_feature_dictionary.md`。
- 特征包括起点价格、1/7/14/28/56 日 as-of 价格、7/14/28/56 日变化、7/14/28/56/90 日历史均值/标准差/中位数、同月历史中位数、历史同价率、历史观测数、季节周期和质量/填充状态。
- leakage、主键、目标日期、分区和 manifest 自动化测试。

**验收标准**

- 每行唯一对应一个可评分预测任务。
- 所有滚动特征严格右闭于预测起点，任何归一化或编码只在当期训练窗拟合。
- 训练、验证、测试之间不存在目标或未来特征穿越。
- 样本计数与 T0 审计可解释地对齐。
- validation 与 final_test 必须分别对齐 T0 的 11,882 和 16,217 条合格配对；若因正式特征资格产生差异，必须逐项解释，不能静默缩样本。
- 连续重建的行数、schema 和内容哈希一致。

**非目标与回滚**

不训练模型，不拟合跨样本统计量，不读取测试目标做特征选择；新增 mart 独立存放，可整体重建，不覆盖 Gold。失败时删除 `data/modeling/p2_forecast_mart`、特征配置和本任务新增测试即可回滚。

### P2-T2：正式基线与滚动回测记分牌

**目标**

在完全相同的评分行上实现三类基线和统一评分器，为后续模型建立不可移动的比较基准。

**计划输出**

- `src/forecast/metrics.py`：WAPE、MAE、sMAPE、pinball loss、coverage、width 与 WIS 的统一实现。
- `src/forecast/build_baseline_scorecard.py`：只读取 validation mart，生成三种基线预测与评分。
- `artifacts/p2/baselines/validation_predictions.parquet`：11,882 行 paired-row 明细。
- `artifacts/p2/baselines/validation_scorecard.json` 与 `docs/p2_baseline_report.md`：产品×跨度、总体和城市序列记分牌。
- 基线定义冻结为：`last_valid_price = origin_price`；`weekly_pattern = weekly_pattern_median`；`historical_seasonal = historical_seasonal_median`。
- 每个产品×跨度按 validation WAPE 选择唯一最佳基线，并将选择冻结供 T3/T5 使用；平局按上述顺序确定。

**验收标准**

- 三个基线均只使用预测起点以前的信息。
- 指标可从预测明细独立复算。
- 后续任何候选模型都在 paired rows 上与最佳基线比较。
- 三个基线必须对 11,882 行全部产生有限正预测，不允许通过缺失预测缩小评分样本。
- 本任务不得读取 `final_test.parquet`；正式测试基线留到模型规格冻结后的 P2-T5 同步生成。
- 评分器以手工小样本单元测试验证公式，并对正式明细抽样复算。

**回滚**

基线产物均可从 T1 mart 重建，不修改 mart。失败时删除 `artifacts/p2/baselines` 与本任务新增报告/测试即可回滚。

### P2-T3：确定性点预测模型

**目标**

在验证期比较低复杂度季节模型与全局树模型，选择最小但稳定的点预测规格。

**候选路线**

- 低复杂度固定候选：damped trend、weekly/seasonal log-space blend。
- 全局岭回归：以 `target_log_return` 为目标，使用训练窗内拟合的缺失中位数、标准化和产品×跨度 one-hot；比较 alpha 与预测收缩系数。
- Hurdle 岭模型：第一阶段预测是否发生超过 1% 的价格变化，第二阶段只在变化样本拟合对数收益，再用变化概率收缩变化幅度。
- 当前环境没有 LightGBM/CatBoost/scikit-learn；本轮不新增重量级二进制依赖，以 NumPy 闭式岭回归完成可复现候选比较。后续如要加入树模型，必须作为新实验版本，不能覆盖本轮冻结结果。

**详细产物**

- `config/p2_models.yaml`：数值特征、alpha、收缩/趋势/混合候选和变化阈值。
- `src/forecast/train_point_models.py`：只读取 train 与 validation，训练、预测、评分和冻结规格。
- `artifacts/p2/models/validation_candidate_predictions.parquet`、`candidate_scorecard.json`、`frozen_point_model.json`。
- `docs/p2_point_model_report.md`：总体、产品×跨度、城市改善占比、最差切片与回退状态。
- 冻结规则：以全验证集 WAPE 选一个全局主模型规格；产品×跨度如果该模型未同时满足 WAPE 正改善和城市改善占比超过 50%，先标记为 baseline fallback。最终发布仍由 T5 测试集决定。

**验收标准**

- 所有选择只基于训练期和 2020 验证期。
- 报告总体提升、产品分布、城市分布、最差案例和运行成本。
- 冻结唯一主模型规格及清晰的产品级基线回退规则，然后才允许打开最终测试集。
- 候选预测必须覆盖 validation 全部 11,882 行，并与 T2 基线保持同一任务键。
- 所有预处理参数只从 train 拟合；模型 JSON 必须足以独立复算 validation 预测。
- 无论复杂模型是否胜出，都要保存完整失败切片；未达标组合默认使用 T2 最佳基线。

**非目标与回滚**

不读取 final_test，不为排行榜提升堆叠大量难解释模型；候选模型失败时保留正式基线作为可交付结果。失败时删除 `artifacts/p2/models` 与本任务新增配置/程序/报告/测试即可回滚。

### P2-T4：概率预测与区间校准

**目标**

围绕冻结点预测模型生成 P10/P50/P90 和 80% 区间，并验证区间是否同时具备覆盖率和信息宽度。

**计划输出**

- `config/p2_probability.yaml`：冻结校准期 2020-01 至 2020-08、时间顺序评估期 2020-09 至 2020-12、分位数与宽度系数候选。
- `src/forecast/calibrate_intervals.py`：使用 T3 路由后的 log-price 绝对残差构建 split conformal 区间。
- 每个产品×跨度优先使用自己的校准残差；不足 100 条时依次回退到跨度层级和全局层级，并显式记录。
- 在后 4 个月上从 `[0.8, 0.9, 1.0, 1.1, 1.25]` 选择全局宽度系数：优先满足总体 75%–85% 覆盖，再取 WIS 最低；若无候选满足，取最接近 80% 覆盖且 WIS 最低者。
- 选择后用 2020 全年 residual refit 最终校准器，供 T5 在 final_test 一次性使用。
- 输出 `artifacts/p2/probability/validation_probability_predictions.parquet`、`calibration_scorecard.json`、`frozen_interval_calibrator.json` 与 `docs/p2_probability_report.md`。

**验收标准**

- 分位数不交叉，价格下界符合业务约束。
- 80% 区间覆盖率目标 75%–85%，并与简单经验区间比较 WIS 和宽度。
- 校准参数不接触最终测试目标。
- P50 必须与 T3 的产品级 validation 路由一致：模型候选使用冻结模型，回退切片使用冻结最佳基线。
- 时间顺序评估只在 2020-09 至 2020-12 上报告，不能用校准期自身覆盖率冒充验证成绩。
- 冻结校准器必须包含每个产品×跨度的 log residual quantile、样本数、回退层级和全局宽度系数。

**回滚**

若概率模型不合格，前端只展示点预测与历史波动提示，不展示虚假精确区间。失败时删除 `artifacts/p2/probability` 与本任务新增配置/程序/报告/测试即可回滚。

### P2-T5：稳定性、误差分析与 Go/No-Go

**目标**

用冻结规格运行最终测试，判断哪些产品和跨度可以进入产品，哪些必须回退到基线。

**计划输出**

- `src/forecast/evaluate_final_test.py`：首次读取 final_test，应用冻结模型、validation 基线路由与冻结校准器，不重新拟合或调参。
- `artifacts/p2/final/final_test_predictions.parquet`、`final_scorecard.json`、`release_matrix.json`。
- `docs/p2_final_model_card.md`：总体与产品×跨度点预测、区间、城市改善占比、月份误差、validation→test 稳定性和限制。
- 最终状态规则：
  - `model_target`：validation 已是模型候选，测试 WAPE 改善 ≥8%、改善城市 >50%、组内覆盖 75%–85%。
  - `model_minimum`：validation 已是模型候选，测试 WAPE 正改善、改善城市 >50%、组内覆盖 75%–85%，但未达 8%。
  - `point_only_model`：前两项点预测门槛通过，但区间覆盖不在 75%–85%；只发布点预测。
  - `baseline_fallback`：validation 已回退，或测试点预测门槛失败；使用冻结最佳基线且不展示模型区间。
- 7 日与 14 日分别统计满足正改善且城市改善 >50% 的产品数；每个跨度至少 6/10 才通过最低产品覆盖门槛。28 日只作扩展结果。

**验收标准**

- 完整执行第 2.6 节门槛。
- 明确区分“达到目标”“最低可用”“仅研究”“回退基线”。
- 不隐去失败产品、极端误差或覆盖率失真。
- 模型、预处理、基线路由、校准分位数和宽度系数必须与 T3/T4 冻结 JSON 完全一致。
- final_test 全部 16,217 行必须产生模型、基线和 validation-routed 预测；任何缺失都使任务失败。
- 最终测试结果不得反向更改模型规格或 validation 路由；测试只用于接受、降级或拒绝。
- 最终测试总体与每组指标必须能从预测明细重新计算。

**回滚**

最终测试失败不修改既有 P1 产品；只阻止对应预测能力发布。失败时删除 `artifacts/p2/final` 与本任务新增程序/报告/测试即可回滚，T3/T4 冻结产物保持不变。

### P2-T6：产品界面集成

**目标**

把通过验收的预测转化为可理解的决策信息，而不是只展示一条模型曲线。

**计划输出**

- `src/forecast/build_forecast_web_data.py`：从 T5 最终明细与发布矩阵生成浏览器大小的只读 JSON，不在前端重算模型。
- `web/public/data/forecast/metadata.json` 与 10 个产品文件：以 2022-05-01 最后一个回测起点为演示快照，包含 Tier A 城市、7/14/28 日发布点预测、合格区间、真实历史结果和产品级测试证据。
- 新增 `/forecast` 工作台：产品、Tier A 城市、跨度筛选；首屏显示发布点预测、P10–P90/不可用状态、真实结果、回测误差与路由解释。
- 同屏展示三个跨度的发布点预测与真实值、发布矩阵证据和数据/模型边界；`point_only_model` 不绘制区间，`baseline_fallback` 明确标注基线来源。
- 在历史监控页与预测页之间增加轻量导航，保持当前深绿/暖白分析工作台视觉和 shadcn/Recharts 组件体系。
- 更新站点 metadata，使其覆盖“历史监控 + 预测回测实验”，不伪装为实时价格或当前预测。

**验收标准**

- 用户能区分实际历史、点预测和不确定区间。
- 不把预测解释为建议报价或自动调价命令。
- 前端类型检查、构建、关键交互和数据回退测试通过。
- 预测数据必须与 T5 16,217 行明细中的 2022-05-01 快照逐值一致；10 个产品文件和 366 条 Tier A 序列可追溯。
- 页面常驻显示 `Historical backtest`、数据截止日、预测起点和“不是当前报价”。
- 发布状态必须遵守 T5：只有 4 个产品×跨度允许 P10/P90，16 个仅 P50，10 个基线回退。
- 加载失败、产品无城市、城市缺某跨度时均有可理解状态；P1 历史页不得回归。

**回滚**

预测模块是独立 `/forecast` 路由和独立 JSON 目录；删除该路由、导航和 `web/public/data/forecast` 即可回滚，P1 历史监控保持可用。本项目按既定计划只做本地交付，不初始化 Git、不创建远程站点、不公开部署。

### P2-T7：文档与本地交付验收

**目标**

完成可复现说明、产品叙事与本地质量验收，并将实际结果回写 Guidebook。

**计划输出**

- `docs/p2_runbook.md`：按 T0→T6 顺序重建审计、mart、基线、模型、区间、最终评估和 Web 数据的命令、输入/输出与失败排查。
- `docs/p2_interview_guide.md`：2 分钟项目叙事、架构、实验设计、真实结果、与 pricing engine 的互补关系、失败切片和下一版路线。
- 将 T5 的最终数字、部分发布矩阵和本地 `/forecast` 演示路径写回 `GUIDEBOOK.md` 与 `README.md`；明确区分设计目标、validation 和 final test。
- 更新 `requirements.txt` 仅在实际新增运行依赖时进行；本轮模型保持 NumPy/Pandas/PyArrow，不加入未使用依赖。
- 运行全部 Python 自动测试、P2 产物哈希/行数复核、TypeScript、forecast 页面 lint、生产构建和本地 GET。
- 输出最终已知问题清单：总体 8% 目标未达、7 日门槛未达、final-test 区间欠覆盖、全站历史 lint 和大 chunk 警告。

**验收标准**

- 新环境可按文档重建数据、模型产物和前端。
- 所有自动测试、构建和关键页面检查通过。
- 文档中的指标可追溯到正式产物，不使用旧诊断结果冒充最终结果。
- Guidebook 与 README 不得写成“模型全面通过”；必须使用 `partial_release` 和 1/3/16/10 状态计数。
- Python 全量测试必须包含 P0、P1、P2，0 失败、0 跳过；TypeScript 与生产构建必须退出码 0。
- 本地 `/`、`/forecast`、forecast metadata 和默认产品 JSON 均返回 200。
- 本地交付完成后保留当前开发预览，不初始化 Git、不创建远程站点、不部署。

**回滚**

文档更新可按文件恢复；不删除任何已验收 P2 数据/模型产物。若最终验收失败，P2 保持“未完成”，并在日志记录具体阻断项，不伪造完成状态。

## 5. 当前执行边界

P2-T0 至 P2-T7 已全部完成，本地交付状态为 `partial_release`。总体 8% 改善、7 日产品覆盖和最终测试区间覆盖没有通过，因此不得把 P2 描述为全面成功；`/forecast` 严格按 1/3/16/10 发布矩阵展示。P3 尚未开工，开始前仍须先写独立执行计划，再实施并追加日志。
