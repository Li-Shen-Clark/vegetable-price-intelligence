# P3 执行计划：价格风险预警

> 状态：P3 v0.1 已完成（`alert_release`，仅历史回放与人工复核触发设计）  
> 前置条件：P0 数据底座、P1 历史价格监控、P2 概率预测均已完成本地验收  
> 产品定位：本模块把历史价格与预测信号转化为“未来 14 天是否可能出现异常涨价”的行动提醒；它是 pricing engine 的上游风险情报层，不替代报价、调价规则或交易执行。

## 1. 执行管理规则

1. 每个任务开始前，先在本文件写清目标、输入、输出、验收标准、非目标和回滚方式。
2. 实施结束后，再向 `P3_执行日志.md` 追加实际动作、产物、验证结果、偏差与下一步；日志只追加，不改写历史记录。
3. 正式结论只使用 P0 Gold 数据、冻结市场映射与 Tier A 正式样本；Tier B 仅作影子诊断。
4. 先冻结事件定义，再生成标签和训练模型；不得为了模型成绩反向修改事件口径。
5. 所有时间统计、编码、归一化、季节阈值与概率校准必须只使用预警起点以前的信息。
6. 2021-01-01 至 2022-06-22 的最终测试期在模型、阈值和 Go/No-Go 规则冻结前不得用于选择。
7. 预警阈值必须由验证集上预设的最大假阳性率约束选出，禁止按最终测试结果人工挑阈值。
8. 未通过发布门槛的模型不得包装为可执行预警；页面必须显示研究状态、历史回放和回退原因。
9. 每项输出均需可复现、可测试、可回滚；不以单次 notebook 或单个案例作为正式证据。

## 2. 冻结范围与核心口径

### 2.1 正式范围

- 产品：沿用 P2 的 10 种核心蔬菜。
- 市场：Tier A 产品—城市组合为正式训练、验证、测试池；Tier B 仅作影子评估；Tier C 排除。
- 预警频率：每周一生成一次历史模拟预警，以避免每日高度重叠样本制造虚假样本量。
- 历史要求：预警起点至少拥有 365 天历史；起点价格最多允许使用前 1 天有效值并保留填充标记。
- 目标窗口：未来 14 个自然日，至少 10 天有非 poor 的真实价格；目标价格不插值、不前向填充。

### 2.2 时间切分

- 训练期：满足历史长度后至 2019-12-31。
- 验证期：2020-01-01 至 2020-12-31，用于模型、超参数和预警阈值选择。
- 最终测试期：2021-01-01 至 2022-06-22，仅在规格冻结后运行一次正式评估。
- 预警起点必须保证完整 14 日目标窗口落在所属数据可用范围内。

### 2.3 主事件定义

对产品—城市—预警起点 `t`：

1. `future_peak_return_14d` 为未来 1–14 日中最高有效批发价相对起点价的涨幅。
2. 历史季节阈值只用 `t` 以前可完成观测的同口径 14 日峰值涨幅计算；优先使用同产品—城市、同日历月的历史 90 分位数，样本不足时按冻结层级回退。
3. 原始显著涨价窗口同时满足：未来峰值涨幅至少 20%，且不低于历史季节阈值。
4. 同一产品—城市在 21 天内连续或重复触发只计为一个事件；第一个合格预警起点记为事件起点，其余记为重复窗口。
5. 正式分类目标为“未来 14 天显著涨价事件起点”；事件提前量为预警起点到未来首次跨越最终事件涨幅阈值的自然日数，范围 1–14 天。
6. 同时生成 15%/20%/25% 绝对涨幅与 90%/95% 历史分位数的敏感性标签，但只有 20%+90% 是主口径。

历史阈值回退层级冻结为：

1. 产品—城市×日历月，历史样本不少于 20；
2. 产品—城市全年，历史样本不少于 52；
3. 产品×日历月，历史样本不少于 100；
4. 产品全年；
5. 全样本历史池。

任一层级仍无足够历史时，该行不进入正式标签与评分。

### 2.4 指标与阈值

- 排序能力：PR-AUC（Average Precision）为主指标，同时报告完整 precision-recall 曲线。
- 固定风险约束：验证集阈值必须满足假阳性率 `FPR = FP / (FP + TN) ≤ 10%`，在可行阈值中优先最大化 recall，再依次选择更高 precision 和更高阈值。
- 同时报告 precision、recall、FPR、误报警占比 `FP / (TP + FP)`、平均/中位提前量、Brier score、top-decile spike recall。
- 所有模型和基线必须在同一 eligible rows 上比较；分母、去重规则与阈值来源必须可复算。

### 2.5 Go/No-Go 原则

最终测试主门槛在训练前冻结：

- `alert_release`：PR-AUC 相对最佳简单基线提升至少 20%，固定验证阈值下测试 FPR 不高于 12%，recall 至少 30%，precision 至少为测试事件率的 2 倍，并且至少命中 20 个事件。
- `research_only`：PR-AUC 高于最佳基线且 recall 至少 20%，但未同时达到发布门槛；页面只允许历史回放和研究提示。
- `no_signal`：排序能力不优于最佳基线，或 recall 低于 20%；产品界面不得展示模型预警，只展示历史波动事实和失败说明。
- 产品级切片必须同时报告；任何单一产品样本不足时不得做成功声明。

## 3. 任务总览

| 任务 | 内容 | 状态 | 核心产物 |
|---|---|---|---|
| P3-T0 | 预警实验契约与可行性审计 | 已完成 | 配置、协议、样本与原始涨价率审计 |
| P3-T1 | 防泄漏事件标签与预警特征集 | 已完成 | 标签 mart、敏感性分析、特征字典 |
| P3-T2 | 简单风险基线与统一分类评分器 | 已完成 | 基线概率、PR 曲线、阈值工具 |
| P3-T3 | 概率预警模型与验证集阈值冻结 | 已完成 | 候选模型、冻结规格、验证报告 |
| P3-T4 | 最终测试、稳定性分析与发布决策 | 已完成 | 测试记分牌、模型卡、发布状态 |
| P3-T5 | 预警产品界面集成 | 已完成 | `/alerts`、历史回放、解释与回退 |
| P3-T6 | 文档、测试与本地交付验收 | 已完成 | 使用说明、验证记录、Guidebook 更新 |

## 4. 任务卡

### P3-T0：预警实验契约与可行性审计

**目标**

在生成正式事件标签前冻结范围、采样频率、目标窗口、质量规则和评估门槛，并验证各时间段有足够周度起点与未来观测。

**输入**

- `data/gold/fact_city_price.parquet`
- `data/gold/coverage_tiers.parquet`
- `config/p2_scope.yaml`
- `README.md`
- `P2_执行计划.md`

**计划输出**

- `config/p3_alert_experiment.yaml`
- `src/alerts/audit_p3_feasibility.py`
- `docs/p3_alert_experiment_protocol.md`
- `docs/p3_feasibility_audit.json`
- `docs/p3_feasibility_audit.md`
- 配置解析、周度起点、目标资格与切分边界测试。

**验收标准**

- 10 种产品、Tier A 池、训练/验证/测试期、周度起点、14 日窗口、质量规则与 Go/No-Go 均有唯一机器可读定义。
- 每种产品在 validation 和 final_test 均有可评估起点；若样本或原始涨价事件不足，必须在审计中标红。
- 审计报告 15%/20%/25% 原始 14 日峰值涨价率，但不把它们当作正式去重事件结果。
- 本任务不训练模型、不使用最终测试选择参数、不发布性能承诺。

**非目标与回滚**

不生成最终事件标签，不接前端，不修改 P0–P2 产物。删除本任务新增配置、审计程序、文档和测试即可回滚。

### P3-T1：防泄漏事件标签与预警特征集

**目标**

按冻结事件定义建立周度产品—城市预警样本，确保季节阈值、事件去重与所有预测特征在起点时真实可得。

**计划输出**

- `config/p3_features.yaml`
- `src/alerts/build_alert_mart.py`
- `data/modeling/p3_alert_mart/{train,validation,final_test}.parquet`
- `data/modeling/p3_alert_mart/manifest.json`
- `docs/p3_event_label_audit.md`
- `docs/p3_feature_dictionary.md`

特征优先复用 P2 的可解释历史信号：起点价、1/7/14/28/56 日变化、7/14/28/56/90 日波动与离散度、历史季节中位数偏离、近期上涨天数、同价率、覆盖和起点填充状态。新增历史事件率必须逐行 expanding 计算。

**验收标准**

- 主键唯一，切分不交叉，未来窗口完整，标签与提前量可从原始价格复算。
- 历史季节阈值严格来自该起点以前已经完成的目标窗口，且记录回退层级和样本数。
- 21 天去重可复现；主标签和 6 个敏感性标签均存在。
- final_test 独立存放；T3 冻结前不得读取其标签做选择。
- 连续重建的 schema、行数与内容哈希一致。

**非目标与回滚**

不训练模型、不调事件阈值、不修改 Gold 和 P2 mart。失败时删除 `data/modeling/p3_alert_mart`、特征配置、程序和本任务文档即可回滚。

### P3-T2：简单风险基线与统一分类评分器

**目标**

在 validation 的同一 eligible rows 上建立不可移动的简单风险基线与完整分类评估工具。

**计划输出**

- `src/alerts/metrics.py`
- `src/alerts/build_alert_baselines.py`
- `artifacts/p3/baselines/validation_predictions.parquet`
- `artifacts/p3/baselines/validation_scorecard.json`
- `artifacts/p3/baselines/validation_pr_curve.parquet`
- `docs/p3_baseline_report.md`

冻结三类基线：训练期产品事件率、逐行过去 52 周产品—城市事件率、仅依赖起点以前数据的近期波动启发式风险分数。

**验收标准**

- 三类基线覆盖全部 validation eligible rows，且不读取 final_test。
- 指标实现经手工小样本测试；PR 曲线端点、无正例和并列分数处理明确。
- 选出唯一最佳基线，选择规则为 validation PR-AUC 最高，平局优先更简单的训练期产品事件率。
- 报告事件率、PR-AUC、固定 FPR recall、precision、误报警占比和产品切片。

**回滚**

基线产物可从 T1 mart 重建；删除 `artifacts/p3/baselines` 与新增程序、报告、测试即可回滚。

### P3-T3：概率预警模型与验证集阈值冻结

**目标**

训练可解释、低依赖的全局概率分类器，在 validation 上选择唯一模型规格和满足 FPR≤10% 的行动阈值。

**候选路线与计划输出**

- 候选：带 L2 正则的逻辑回归；数值缺失中位数、标准化与产品/月份编码仅从 train 拟合。
- 网格：有限的正则强度与正例权重；不为排行榜堆叠黑盒模型。
- `config/p3_models.yaml`
- `src/alerts/train_alert_models.py`
- `artifacts/p3/models/validation_candidate_predictions.parquet`
- `artifacts/p3/models/candidate_scorecard.json`
- `artifacts/p3/models/frozen_alert_model.json`
- `docs/p3_alert_model_report.md`

**验收标准**

- 训练和所有预处理只使用 train；候选选择和阈值选择只使用 validation。
- 每个候选覆盖相同 validation 行；同时比较最佳简单基线。
- 冻结模型 JSON 足以独立复算概率，包含特征、编码、预处理、系数、验证阈值和阈值选择证据。
- 报告总体及产品切片、PR 曲线、概率分布、校准、最差案例与预期误报负担。
- 在完成冻结前不得读取 final_test 标签。

**回滚**

候选失败时保留 T2 基线和 `no_signal` 路径；删除 `artifacts/p3/models` 与新增配置、程序、报告、测试即可回滚。

### P3-T4：最终测试、稳定性分析与发布决策

**目标**

用完全冻结的模型和验证阈值一次性评估最终测试期，给出可审计的 `alert_release`、`research_only` 或 `no_signal` 决策。

**计划输出**

- `src/alerts/evaluate_alert_final_test.py`
- `artifacts/p3/final/final_test_predictions.parquet`
- `artifacts/p3/final/final_pr_curve.parquet`
- `artifacts/p3/final/final_scorecard.json`
- `artifacts/p3/final/release_decision.json`
- `docs/p3_final_model_card.md`

**验收标准**

- 不重新拟合模型、不调整阈值、不更改事件定义。
- 完整执行第 2.5 节门槛，并报告 validation→test 漂移、每产品、月份、事件强度、提前量和敏感性切片。
- top-decile spike recall 只在 eligible actual events 中计算并明确分母。
- 报告最少 10 个真阳性/假阳性/漏报案例；不足时列出全部。
- 发布决定机器可读，失败时页面回退规则明确。

**回滚**

删除 `artifacts/p3/final` 和新增报告即可；不得反向修改 T0–T3 冻结口径。

### P3-T5：预警产品界面集成

**目标**

在现有本地工作台新增 `/alerts`，把最终测试结果做成可解释的历史预警回放，而非伪装成实时数据。

**计划输出**

- 按产品拆分的浏览器数据与 manifest。
- `/alerts` 页面与导航入口。
- 产品、城市、历史预警起点筛选；风险概率、阈值、实际结果、提前量、峰值涨幅和数据质量展示。
- “发生了什么、严重程度、局部还是区域性、涉及哪些产品/城市、可采取什么动作”的解释模块。
- 全局与切片发布状态、误报率和研究限制说明。

**验收标准**

- 页面明确标记为 2014–2022 历史回放，不声称当前实时预警。
- `research_only`/`no_signal` 时不显示可执行式推荐；只展示验证证据与历史事实。
- 页面不读取 Parquet 或超大原始数据，筛选与图表在常见桌面/移动宽度可用。
- 生产构建、静态资源、关键路由和数据契约验证通过。

**回滚**

删除 `/alerts` 路由和对应导出数据，恢复导航即可；不影响 P1/P2 页面。

### P3-T6：文档、测试与本地交付验收

**目标**

统一收口 P3 的运行方式、证据、限制、产品定位和下一阶段建议，完成本地可复现交付。

**计划输出**

- 更新 `README.md`、公开案例文档和 P3 计划状态。
- 完整测试、生产构建、关键页面 GET 与产物存在性核验。
- P3 最终日志条目与本地交付清单。

**验收标准**

- 从 Gold 到标签、基线、模型、最终测试和前端导出的命令顺序可重跑。
- 自动化测试全部通过；前端构建与 `/`、`/forecast`、`/alerts` 本地响应通过。
- 文档准确引用真实结果，不残留“待完成”或未经验证的成功表述。
- 不初始化 Git、不发布外网、不接实时数据源；这些均需后续单独授权。

**回滚**

文档更改可逐文件回退；P3 产物均独立存放，不覆盖 P0–P2 正式产物。
