# P4 执行计划：采购情景引擎

> 状态：P4 v0.1 已完成，`scenario_release`（仅历史回放与参数化采购情景）  
> 前置条件：P0 数据底座、P1 历史监控、P2 概率预测与 P3 风险预警均已完成本地验收  
> 产品定位：本模块把 P2 已发布的模型或基线价格、城市距离、运输与损耗假设以及数据/预测风险组合成可解释的候选采购来源排名；它是 pricing engine 的上游采购决策情报层，不替代供应商确认、询价、交易执行或自动调价。

## 1. 执行管理规则

1. 每个任务开始前，先在本文件写清目标、输入、输出、验收标准、非目标和回滚方式。
2. 实施结束后，再向 `P4_执行日志.md` 追加实际动作、产物、验证结果、偏差与下一步；日志只追加，不改写历史记录。
3. 不修改 P0–P3 已冻结的事实表、模型、阈值、发布矩阵和最终测试产物。P4 新增数据均独立存放并可整体重建。
4. 正式点价格必须逐组遵守 P2 `release_matrix.json`：模型发布组使用冻结模型点预测，回退组使用冻结基线。P4 不得把基线改名为模型预测。
5. 风险缓冲的历史残差只使用情景日期以前已经完成的预测任务，要求 `target_date < scenario_origin_date`；不得使用当前情景或未来目标值校准风险。
6. P2 未发布的区间不得在 P4 重新包装为 P10/P90。只有 `release_interval=true` 的组可使用并展示发布区间；其余使用明确标注的经验残差风险缓冲。
7. 距离、运费、损耗、采购量、可用性和道路折算均为可编辑情景假设。输出只能称为候选采购来源或采购情景，不能称为验证后的供应商、真实节省或最优采购执行。
8. 所有成本使用同一单位：价格为元/公斤，采购量为到货可用公斤，运输费率为元/(公斤·公里)，距离为公里，成本为元。
9. 每项输出均需可复现、可测试、可回滚；不以单个默认场景或页面截图作为验收证据。

## 2. 冻结范围与核心口径

### 2.1 正式范围

- 产品：沿用 P2 的 10 种核心蔬菜。
- 候选城市：产品对应的 Tier A 产品—城市组合，并且在情景起点有 P2 已发布点价格、城市坐标和满足阈值的数据可靠性。
- 默认历史情景日期：2022-05-01，与 `/forecast` 的最后一个完整回测起点一致。
- 预测跨度：7、14、28 个自然日全部保留；28 日作为默认中期采购规划跨度，14 日作为战术情景，7 日作为近期补货情景。
- 目标城市：117 城维度中的任一城市。目标城市不要求自身是该产品的 Tier A 来源城市；没有本地合格报价时，不计算本地节省，只保留外地候选排名。
- 候选来源：默认排除目标城市后给出 Top 3 外地来源；若目标城市自身有合格报价，则单独展示本地基准，不与外地候选混入同一名次。

### 2.2 城市坐标与距离

P4 不直接聚合旧市场坐标。冻结原因是 `market_mapping.csv` 中只有 200/246 个正式市场有坐标，且部分 legacy 坐标与其正式城市明显错位；简单质心、medoid 或记录最多市场均可能产生错误运输距离。

- 新建独立 `reference/dim_city_geo.csv`，不覆盖 P0 的 `reference/dim_city.csv`。
- 116 个同名城市使用旧研究项目 `09_map/CityList.csv` 的城市中心坐标快照。
- `大理州弥渡县` 与源表 `大理白族自治州` 粒度不一致，使用正式映射中唯一市场 `mk115` 的坐标作为县级市场锚点，并记录特殊规则。
- 23 个 `unmapped_new_city` 不进入正式 117 城范围，也不进入 P4 候选池。
- 距离先计算城市锚点之间的 Haversine 直线距离；默认乘 1.25 的可编辑道路折算系数得到“估算运输距离”。该数值不是公路 API 路线或运输时效。
- 坐标表必须包含 `coordinate_method`、`coordinate_source`、`source_city_name`、`alias_rule`、`qa_status` 和 `geo_dimension_version`。

### 2.3 发布点价格与区间

每个产品×跨度使用 P2 T5 冻结发布路线：

- `model_target` / `model_minimum`：发布模型点价格；同时允许展示已校准 P10/P90。
- `point_only_model`：发布模型点价格；不得展示 P10/P90，风险项使用历史残差替代。
- `baseline_fallback`：发布 `last_valid_price` 或 `historical_seasonal` 基线；不得称为模型预测，也不得展示模型区间。

页面和数据必须保存 `release_status`、`release_point_source`、`price_input_label`、`interval_status` 和 `risk_basis`。

### 2.4 数据可靠性

- 以 P2 预测起点实际使用的 `origin_price_date` 连接 Gold 城市日事实的 `source_reliability_score`。
- 如果该日没有匹配分数，允许使用起点以前最近 28 天的中位数，并标记回退；仍缺失则排除该候选。
- 默认最低可靠性阈值为 0.65，页面允许在 0.50–0.95 之间调整。
- `source_reliability_score` 是报价数据质量证据，不等于预测误差概率，只能用于候选过滤和有界风险放大。

### 2.5 风险缓冲

对来源城市 `i`、产品 `v`、跨度 `h`：

```text
若 P2 发布合格区间：
    base_uncertainty_i = max(P90_i - P50_i, 0)
否则：
    q80_log_error_vh = Q80(|log(actual / released_point)|)
    base_uncertainty_i = released_point_i × (exp(q80_log_error_vh) - 1)

reliability_multiplier_i
    = clip(1 + reliability_gamma × (1 - source_reliability_score_i), 1, 1.5)

risk_penalty_i
    = risk_aversion_lambda × base_uncertainty_i × reliability_multiplier_i
```

- 经验残差按产品×跨度×实际发布路线计算，最少 30 行；不足时依次回退到产品×跨度、产品、跨度、全局。
- 残差只使用 `target_date < scenario_origin_date` 的 P2 final-test 历史回测行。
- 默认 `risk_aversion_lambda=0.50`，页面范围 0–1.5；默认 `reliability_gamma=1.0` 并在 v0.1 中固定。
- 风险缓冲用于情景排序，不代表校准后的置信区间；页面必须显示其来源。

### 2.6 到岸成本

默认参数：

- 到货可用采购量：10,000 公斤；
- 运输费率：0.0025 元/(公斤·公里)；
- 损耗率：8%；
- 最大估算运输距离：1,500 公里；
- 道路折算系数：1.25；
- 风险偏好：0.50；
- 最低来源可靠性：0.65。

对每个来源城市：

```text
估算运输距离 = Haversine直线距离 × 道路折算系数
单位运输成本 = 估算运输距离 × 运输费率
单位风险调整采购成本 = 发布点价格 + 单位运输成本 + 风险惩罚
单位到岸成本 = 单位风险调整采购成本 / (1 - 损耗率)
到岸总成本 = 单位到岸成本 × 到货可用采购量
```

损耗成本以为了获得相同可用到货量而需要增购的成本体现，避免简单加 `价格×损耗率` 低估高损耗情景。所有候选和本地基准必须使用同一公式。

### 2.7 敏感性分析

冻结 36 个组合：

- 运输费率：0.0015、0.0025、0.0035、0.0045；
- 损耗率：3%、8%、15%；
- 风险偏好：0、0.5、1.0。

对当前目标城市、产品和跨度报告：每个候选进入 Top 3 的次数/比例、成为第一名的次数/比例、默认排名和成本范围。敏感性分析不改变正式发布价格和残差口径。

### 2.8 产品与证据边界

- P4 默认 28 日不改变 P2 原先以 7/14 日为主门槛的历史评估设计；两者分别是采购页面默认值与模型验收口径。
- 7 日 `last_valid_price` 必须显示“当前价格延续（基线），不是模型预测”。
- 28 日黄瓜等 `historical_seasonal` 回退必须显示“历史季节基线，不是模型预测”。
- 没有真实采购量、库存、产能、供应承诺、道路路线、运输时效、实时运费或成交价，因此排名不能解释为可执行采购订单。
- `source_reliability_score` 低或风险缓冲高的低价城市不得通过隐藏风险项进入推荐。
- P4 v0.1 只使用历史回测快照，不产生 2026 年当前采购建议。

## 3. 任务总览

| 任务 | 内容 | 状态 | 核心产物 |
|---|---|---|---|
| P4-T0 | 城市坐标与采购实验契约审计 | 已完成 | 配置、城市坐标维度、坐标异常审计、协议 |
| P4-T1 | 发布路线与无前视风险缓冲数据 | 已完成 | 风险残差表、采购快照 mart、字段字典 |
| P4-T2 | 到岸成本与候选来源排名引擎 | 已完成 | 可复算计算引擎、默认场景结果、单元测试 |
| P4-T3 | 参数敏感性与排名稳定性 | 已完成 | 36 组合敏感性、稳定性报告 |
| P4-T4 | 产品界面集成 | 已完成 | `/procurement`、浏览器数据、导航与状态 |
| P4-T5 | 文档与求职叙事 | 已完成 | Guidebook、README、runbook、面试说明 |
| P4-T6 | 全量测试与本地交付验收 | 已完成 | 测试、构建、页面响应与交付清单 |

## 4. 任务卡

### P4-T0：城市坐标与采购实验契约审计

**目标**

冻结城市锚点、距离、候选池、成本单位、风险规则和产品边界，并证明 117 城均有唯一可用坐标且旧市场坐标不会进入正式距离计算。

**输入**

- `reference/dim_city.csv`
- `reference/market_mapping.csv`
- `/Users/lishen/Downloads/vegProject/09_map/CityList.csv`
- `data/gold/coverage_tiers.parquet`
- `artifacts/p2/final/release_matrix.json`
- `artifacts/p2/final/final_test_predictions.parquet`
- `README.md`

**计划输出**

- `reference/source_city_coordinates.csv`
- `reference/dim_city_geo.csv`
- `config/p4_procurement.yaml`
- `src/procurement/build_city_geo.py`
- `src/procurement/audit_p4_feasibility.py`
- `docs/p4_procurement_protocol.md`
- `docs/p4_coordinate_audit.csv`
- `docs/p4_coordinate_audit.md`
- `docs/p4_feasibility_audit.json`
- `docs/p4_feasibility_audit.md`
- 坐标、配置、候选池与发布矩阵契约测试。

**验收标准**

- `dim_city_geo.csv` 恰好 117 行，`city_id` 唯一，与 `dim_city.csv` 一一对应，坐标有限且位于合理中国范围。
- 116 个同名城市与 1 个大理特殊规则均可追溯；不存在静默模糊匹配。
- 市场坐标完整率、相对城市锚点偏差和异常清单可复算；正式距离代码不读取市场坐标。
- 10 种产品、三个跨度、P2 1/3/16/10 发布矩阵和默认参数在机器可读配置中唯一冻结。
- 本任务不计算最终推荐，不修改 P0–P3 产物。

**非目标与回滚**

不补造市场级坐标，不调用公路地图 API，不修改原市场映射。删除本任务新增的 geo/config/审计产物即可回滚。

### P4-T1：发布路线与无前视风险缓冲数据

**目标**

建立 2022-05-01 历史采购快照，逐行连接 P2 发布点、合格区间、起点数据可靠性和情景日期以前的经验残差风险缓冲。

**计划输出**

- `src/procurement/build_procurement_mart.py`
- `data/modeling/p4_procurement_mart/scenario_snapshot.parquet`
- `data/modeling/p4_procurement_mart/residual_calibration.parquet`
- `data/modeling/p4_procurement_mart/manifest.json`
- `docs/p4_data_dictionary.md`
- `docs/p4_risk_calibration.md`
- mart 主键、发布路线、无前视残差、可靠性连接与风险来源测试。

**验收标准**

- 快照只包含 P2 正式 10 产品、7/14/28 日和有效 Tier A 来源；主键唯一。
- `released_point_prediction`、区间和发布状态与 P2 final artifact 逐值一致。
- 每个候选有城市坐标、有限正点价格、可靠性分数及风险基础；没有合格区间的行不产生伪 P10/P90。
- 残差校准行全部满足 `target_date < 2022-05-01`，且记录层级、样本数和 Q80。
- 重建的行数、schema 和内容哈希稳定。

**回滚**

mart 独立于 Gold/P2，可删除 `data/modeling/p4_procurement_mart`、程序和文档后重建。

### P4-T2：到岸成本与候选来源排名引擎

**目标**

实现单位一致、可解释、可复算的来源过滤、距离、运输、损耗、风险和 Top 3 排名逻辑。

**计划输出**

- `src/procurement/engine.py`
- `src/procurement/build_default_scenarios.py`
- `artifacts/p4/default_scenarios.json`
- `docs/p4_cost_methodology.md`
- Haversine、过滤、成本分解、本地基准、排序稳定性和边界输入测试。

**验收标准**

- 手工小样本可复算距离、运输成本、风险惩罚、损耗调整和总成本。
- 排名仅使用通过最大距离与可靠性门槛的候选；并列按单位到岸成本、可靠性降序、城市 ID 固定排序。
- 本地基准单独显示；节省仅在本地基准存在时计算。
- 基线回退组保持准确文案和来源；所有总成本随采购量线性变化，单位排名不受采购量影响。
- 非法参数、无候选和无本地基准返回明确状态，不产生看似有效的零成本。

**回滚**

引擎只读取 T1 mart，不修改上游。删除本任务新增代码、artifact、文档和测试即可回滚。

### P4-T3：参数敏感性与排名稳定性

**目标**

用冻结 36 组合量化候选排名对运输费率、损耗和风险偏好的敏感程度，避免把单一默认参数结果包装成稳定推荐。

**计划输出**

- `src/procurement/run_sensitivity.py`
- `artifacts/p4/sensitivity_summary.json`
- `artifacts/p4/sensitivity_rows.parquet`
- `docs/p4_sensitivity_report.md`
- 组合数、Top 3 频率、第一名频率、成本范围和确定性测试。

**验收标准**

- 每个可评估默认场景运行完整 36 个组合；参数组合无重复、输出主键唯一。
- Top 3 频率和第一名频率分母正确，范围为 0–1；每组排名可由 T2 引擎复算。
- 报告至少一个稳定案例和一个敏感案例；只描述参数化情景行为。

**回滚**

敏感性产物可从 T1 mart 和 T2 引擎重建；删除本任务新增产物即可回滚。

### P4-T4：产品界面集成

**目标**

新增 `/procurement` 工作台，让用户选择目标城市、产品、跨度、采购量、最大距离、运输费率、损耗率、风险偏好和可靠性阈值，并即时查看本地基准、Top 3 候选与敏感性。

**计划输出**

- `src/procurement/build_procurement_web_data.py`
- `web/public/data/procurement/metadata.json`
- `web/public/data/procurement/manifest.json`
- `web/public/data/procurement/products/*.json`
- `web/app/procurement/page.tsx`
- P1/P2/P3/P4 页面导航更新。

**页面要求**

- 首屏直接显示核心筛选与至少一个完整候选结果，不放营销式 hero。
- 默认使用 28 日；7/14/28 日均可切换并显示发布路线。
- 候选表拆分点价格、估算距离、运输成本、损耗调整、风险惩罚、单位到岸成本和总成本。
- 显示本地基准、Top 3、风险来源、数据可靠性、区间状态和相对本地模拟节省。
- 对 `last_valid_price` 和 `historical_seasonal` 使用明确的非模型文案。
- 敏感性区域显示 Top 3 进入频率、第一名频率和参数范围。
- 常驻显示 Historical scenario、数据截止日、城市中心距离假设、无供应能力/实时运费证据和不执行采购。
- 包含加载、数据错误、无候选、无本地基准和参数越界状态；支持桌面与窄屏。

**验收标准**

- 浏览器数据与 T1 mart、T2 默认场景和 T3 敏感性逐值可追溯。
- 页面交互改变运输费率、损耗、风险偏好、距离或可靠性时，成本与排名按冻结公式响应。
- 前端构建通过；`/`、`/forecast`、`/alerts` 不回归。
- 本项目延续既定本地交付，不公开部署。

**回滚**

删除 `/procurement`、导航入口和 `web/public/data/procurement` 即可回滚，前三个页面保持可用。

### P4-T5：文档与求职叙事

**目标**

把实际 P4 结果、方法、边界和与 pricing engine 的互补关系写入项目主文档，并形成可复现和可面试讲解的交付材料。

**计划输出**

- `docs/p4_runbook.md`
- `docs/p4_interview_guide.md`
- `docs/p4_delivery_checklist.md`
- 更新 `README.md`、公开案例文档和 `docs/data_dictionary.md`。

**验收标准**

- 文档区分模型点预测、基线点价格、合格区间和经验风险缓冲。
- 所有默认参数、公式、来源、历史日期和限制与机器产物一致。
- 面试叙事能说明从预测证据到采购情景的转换、失败切片如何保留，以及为何 P4 与 pricing engine 互补。
- 重建顺序和失败排查可在新环境执行。

**回滚**

文档更新只追加 P4 状态，不改写 P0–P3 历史结论；可按新增段落回退。

### P4-T6：全量测试与本地交付验收

**目标**

完成 P4 全量数据契约、计算、浏览器数据、生产构建和本地响应验收，并给出最终放行状态。

**计划输出**

- 运行全部 Python 自动测试并记录通过数。
- 运行前端 lint/类型检查与生产构建。
- 本地检查 `/`、`/forecast`、`/alerts`、`/procurement` 和采购数据端点。
- 完成 `docs/p4_delivery_checklist.md`、执行日志和主文档状态。

**验收标准**

- P0–P4 全量自动测试通过；P4 核心公式有独立手算测试。
- 生产构建识别四条产品路由；关键本地页面和 JSON 返回 HTTP 200。
- 默认场景至少有三个合格外地候选；无候选与无本地基准路径均被测试。
- 所有产物哈希、行数、发布状态计数和坐标行数可复算。
- 最终状态只能是 `scenario_release`、`research_only` 或 `blocked`；`scenario_release` 仍只代表历史参数化情景可演示。
- 不初始化 Git，不提交、不推送、不公开部署。

**回滚**

P4 全部产物独立；删除 `src/procurement`、P4 配置/数据/artifact/docs、`/procurement` 与导航入口即可回到 P3 完成状态。

## 5. P4 Go/No-Go

`scenario_release` 需要同时满足：

- 117 城坐标一一对应，坐标来源和特殊规则可追溯；
- 10 产品×3 跨度的发布路线与 P2 完全一致；
- 所有风险残差无前视，未发布区间不被伪造；
- 到岸成本和敏感性可由独立测试复算；
- 页面显式保留基线、历史回放和数据边界；
- 全量测试、生产构建和本地响应通过。

若坐标或发布路线不可靠，状态为 `blocked`；若公式可运行但候选覆盖或稳定性不足，状态为 `research_only`。任何状态都不授权实时采购或自动定价。
