# P5 前置执行计划：经济学/Pricing Gate、作品集叙事与 GitHub 基线

> 状态：实施中  
> 目标：在进入 P5 价格冲击传播前，把 P0–P4 冻结为一份经济学逻辑明确、Pricing 岗位相关、适合安全同步到 GitHub 的可复现作品集基线。  
> GitHub 策略：先创建私有仓库 `vegetable-price-intelligence`；严格不上传任何原始、生成或演示数据，只同步代码、配置、测试和方法文档；不伪造历史提交日期，不公开部署网站。

## 1. 执行管理规则

1. 每项任务必须先在本计划冻结目标、输出、验收和回滚，再实施，再向 `P5_前置执行日志.md` 追加实际结果。
2. 不修改 P0–P4 已冻结的模型、阈值、事实表、发布矩阵和正式评估结论。
3. 经济学/Pricing Gate 从本阶段开始是所有后续阶段的必需门槛；P5 计划、执行日志、产品页面和面试叙事均须逐项回应。
4. GitHub 初始历史必须诚实：当前文件作为 P0–P4/P4.1 基线提交，不回填虚假日期或制造虚假开发时间线。
5. 不执行 `git add .`，直到忽略规则、大文件、凭据和数据许可边界通过审计。
6. 不删除本地原始数据；大型数据只从 Git 跟踪范围排除。
7. 网站继续本地运行，不创建公开部署；GitHub 仓库初始为 private。

## 2. 任务总览

| 任务 | 内容 | 状态 | 核心产物 |
|---|---|---|---|
| PRE5-T0 | 冻结前置范围并审计 Git/GitHub 与仓库体积 | 已完成 | 审计结果、纳入/排除策略 |
| PRE5-T1 | 建立 Economics & Pricing Stage Gate | 已完成 | 阶段门槛、P0–P4 回顾、Guidebook 规则 |
| PRE5-T2 | 整理 GitHub-safe 作品集包 | 已完成 | `.gitignore`、数据访问说明、作品集验证脚本 |
| PRE5-T2R | 按用户要求改为严格无数据 Git 基线 | 已完成 | 扩大忽略范围、移除可推送历史中的数据、修正文档与验证 |
| PRE5-T2L | 按用户要求将全部执行日志改为仅本地保存 | 已完成 | 忽略日志、清除可推送历史中的日志、增加自动防线 |
| PRE5-T3 | 完成 P4.1 Pricing/Economics 网站叙事 | 已完成 | 首页决策链、采购 Pricing Bridge、叙事测试 |
| PRE5-T4 | 全量回归与 GitHub 基线同步 | 进行中（等待最终 Desktop 同步） | 测试、构建、私有远端、基线提交与标签 |
| PRE5-T5 | 冻结 P5 进入门槛 | 已完成（本地） | 最终清单、日志、P5 readiness 结论 |

## 3. 任务卡

### PRE5-T0：仓库与远端可行性审计

**目标**

确认 Git 状态、GitHub CLI 身份、已有远端名称冲突、目录体积、大文件、构建缓存、潜在凭据和数据公开风险，形成可执行的跟踪边界。

**计划输出**

- 在执行日志记录 Git/CLI 状态和大文件事实；
- 冻结跟踪范围：代码、配置、测试、Markdown 文档和非数据 Web 资源；
- 冻结排除范围：原始/重复数据、Gold/Silver/Bronze 与 modeling 大表、本地压缩包、依赖、构建缓存、环境变量、系统文件；
- 若 GitHub 身份不可用，仍完成全部本地前置工作并把远端同步标记为唯一阻塞项。

**验收标准**

- 不存在未经审计的大文件进入暂存区；
- 不显示或提交令牌、密码、私钥或 `.env`；
- 私有仓库名称和默认分支可唯一确定。

### PRE5-T1：Economics & Pricing Stage Gate

**目标**

把用户要求的经济学与 Pricing 相关性从一次性说明变成每阶段的强制治理流程。

**计划输出**

- `docs/economics_pricing_stage_gate.md`；
- 对 P0–P4 做回顾矩阵；
- 为 P5 及以后冻结七问：决策问题、经济机制、单位/约束、识别或比较基准、Pricing 接口、不可声称内容、页面表达；
- 更新 `README.md` 与公开方法文档的执行规则。

**验收标准**

- 每问均有 Pass/Conditional/Fail 规则和所需证据；
- 明确当前没有销量，不能估计需求弹性、最优零售价或已实现利润；
- P5 在写执行计划前必须先完成本 Gate 的阶段假设卡。

### PRE5-T2：GitHub-safe 作品集包

**目标**

让代码仓库保持小而可审阅，严格不包含数据，并诚实说明填充网站与完整数据回归的恢复条件。

**计划输出**

- `.gitignore`；
- `docs/data_access_and_reproducibility.md`；
- `data/README.md`（作为被忽略数据目录中的跟踪说明）；
- `scripts/verify_portfolio_bundle.py` 与对应测试；
- README 区分“作品集模式”和“完整数据模式”。

**纳入 Git 的核心内容（严格无数据修订）**

- `src/`、`tests/`、`config/` 和 Markdown 方法文档；
- `web/` 源码、依赖锁和非数据静态资源；
- Guidebook 与各阶段计划；全部执行日志仅保留在本机。

**排除 Git 的本地内容（不论大小）**

- `OneDrive_*.zip`、`dataSource/`；
- `data/` 中所有生成表，除不含数据的 `data/README.md`；
- `artifacts/`、`baselines/`、`web/public/data/`；
- `reference/*.csv`、`docs/**/*.csv`、`docs/**/*.json`、`docs/evidence/`、`unmatched_markets.csv`；
- 全部 Parquet 和其他模型预测/评估明细；
- `web/node_modules`、构建目录、缓存；
- `.env*`、密钥、Python 缓存、macOS 文件。

**验收标准**

- 暂存候选中不存在超过 50 MiB 的单文件；
- 作品集验证脚本检查代码/文档结构、无数据规则、文件大小和敏感路径；
- README 明确 GitHub clone 不能运行数据驱动页面或全量测试，必须由授权用户在本地恢复数据。

### PRE5-T2R：严格无数据 Git 基线修订

**触发原因**

用户明确要求“不要把数据上传”。此前计划允许约 30MB 浏览器演示数据和轻量 artifacts；该口径立即作废，尚未发生远端上传。

**计划动作**

1. 扩大 `.gitignore`，覆盖原始、生成、衍生、演示和评估明细数据；
2. 仅从 Git 索引移除这些文件，本地文件原位保留；
3. 修正作品集验证脚本、测试、README 与数据访问说明；
4. 重写尚未推送的根提交，使任何将被推送的历史都不包含数据 blob；
5. 复审全部可推送路径、扩展名、总大小和最大文件。

**验收标准**

- `git ls-tree -r HEAD` 不包含 `artifacts/`、`baselines/`、`dataSource/`、`web/public/data/`、数据 CSV/Parquet 或数据型 docs；
- 本地上述文件均仍存在；
- GitHub 远端仍不存在，因而变更前的数据从未上传；
- 代码、配置、测试和方法文档保持可审阅，前端生产构建仍可完成。

### PRE5-T2L：执行日志仅本地保存

**触发原因**

用户明确要求执行日志不要上传。当前远端仍不存在，因此可以在首次发布前彻底调整边界。

**计划动作**

1. 以 `*执行日志.md` 覆盖 P0–P5 及未来阶段日志；
2. 日志文件继续在本机按“先计划、后追加”的规则维护，不删除内容；
3. 从 Git 索引移除六份现有日志，并把 `main` 重写为不含日志的单一诚实基线；
4. README 与数据访问文档明确“计划公开、日志本地”；
5. 自动验证同时检查当前候选和 `HEAD` 可达历史，拒绝任何执行日志。

**验收标准**

- `git rev-list --objects main` 不含文件名以 `执行日志.md` 结尾的路径；
- 六份本地日志仍存在且内容不变；
- 无 GitHub 远端、无 push；
- GitHub Desktop 的标准 `Publish repository` 只会发布通过审计的 `main`。

### PRE5-T3：P4.1 Pricing/Economics 网站叙事

**目标**

在不增加营销式 hero、不改变现有分析结果的前提下，让招聘方从首页第一屏理解“市场信号 → 预测/预警 → 风险调整采购成本 → Pricing Engine 输入”。

**计划输出**

- 首页加入紧凑的 Market-to-Pricing 决策链和项目定位；
- 首页把四个产品模块解释为一个决策系统，而非并列 Dashboard；
- 采购页首屏加入 `Pricing Decision Bridge`：风险调整成本、毛利/成本复核触发器、替代来源询价，不输出零售价；
- 采购页把运输、损耗、风险偏好和敏感性映射为空间摩擦、cost-to-serve、风险厌恶和比较静态；
- 新增前端内容契约测试，并保持加载/错误/空状态和窄屏设计。

**验收标准**

- 首页首屏明确出现 Pricing 决策链及与 pricing engine 的互补关系；
- 采购页无需滚动到页尾即可理解输出如何服务 Pricing；
- 不出现“最优供应商”“最优零售价”“真实节省”或自动调价承诺；
- P4 页面定向 lint、生产构建和关键本地路由通过。

### PRE5-T4：全量回归与 GitHub 基线同步

**目标**

验证 P0–P4/P4.1 后初始化 Git，按诚实的当前逻辑提交，创建私有 GitHub 仓库并同步基线。

**计划动作**

1. 运行全部 Python 测试、作品集验证、P4.1 内容测试和前端生产构建；
2. 初始化 `main` 分支；
3. 检查暂存候选的文件数、总大小和最大文件；
4. 建立一个严格无数据的 P0–P4/P4.1 基线提交；
5. 通过已登录 GitHub 身份创建 private `vegetable-price-intelligence`；
6. 推送 `main` 并创建、推送 `v0.1.0` 标签；
7. 不创建虚假历史、不改为 public、不部署网站。

**验收标准**

- 测试和构建通过；
- Git 跟踪范围与 T2 一致，无大型原始数据或凭据；
- 远端为 private，`main` 与 `v0.1.0` 可在远端解析到同一基线提交。

### PRE5-T5：P5 readiness 冻结

**目标**

形成 P5 开工前的单一结论，保证下一阶段先回答经济学问题再建模。

**计划输出**

- `docs/p5_readiness_checklist.md`；
- 更新本计划状态、README 和追加日志；
- 明确 P5 的第一项工作必须是传播问题的经济机制、反事实、候选边约束与 Pricing 用途，不直接开始网络估计。

**验收标准**

- PRE5-T0–T5 全部完成，或唯一阻塞项被明确记录；
- P5 Gate 至少覆盖空间套利、共同冲击、方向识别、FDR/稳定性和样本外增益；
- GitHub 基线和本地工作区一致。

## 4. 回滚

- 所有说明、测试和页面改动可按本阶段文件范围回退，不影响 P0–P4 冻结数据与模型。
- `.gitignore` 只影响跟踪，不删除本地文件。
- 若已创建私有远端但同步失败，保留本地 Git 历史并在日志记录，不删除远端或重写历史。
