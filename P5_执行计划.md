# P5 执行计划：价格冲击传播与 Pricing 风险扩散

> 阶段目标：在控制全国共同价格变化后，识别城市之间是否存在稳定、可复现、具有样本外预测增益的价格冲击 lead-lag 关系，并把它转化为 Pricing 人工复核的提前量与暴露范围。
> 产品边界：只做 2014–2022 历史研究和本地演示；不声称因果贸易流、实时风险、自动调价或实际利润改善。
> 发布边界：GitHub 只含代码、非数据配置、测试、方法文档和计划；所有数据、JSON/CSV/Parquet artifacts、浏览器数据与 `P5_执行日志.md` 仅保留本机。

## 1. 执行规则

1. 每个任务先在本计划冻结目标、输入、口径和验收标准，再执行，再向本地 `P5_执行日志.md` 追加结果。
2. Economics & Pricing Gate G1–G7 每个任务都要复核；技术指标不能代替经济解释。
3. final test（2021-01-01 至 2022-06-22）在模型、阈值和候选边冻结前不得查看结果；只允许一次正式评估。
4. 不对所有城市对和所有滞后穷举后挑选显著结果。候选边先由地理规则确定，统计检验再做 BH-FDR。
5. 共同冲击控制是传播检验的硬前置；原始同步涨价不能被命名为城市间传播。
6. P2/P3/P4 产物只读，不覆盖既有模型、阈值、发布矩阵或情景结果。
7. 若传播边不稳定或没有样本外增益，降级为“共同冲击与区域暴露”模块，不发布方向网络。
8. 每次提交前运行严格无数据/无日志 verifier；本阶段不部署网站。

## 2. Economics & Pricing Hypothesis Card

| Gate | P5 冻结答案 | 所需证据 |
|---|---|---|
| G1 决策 | Pricing、采购或供应团队在来源城市出现异常涨价后，决定哪些目标城市需要提前进入成本、供应和报价复核 | 城市暴露、预计滞后、历史命中与人工复核动作 |
| G2 机制 | 空间套利、信息扩散和潜在贸易联系可能形成 lead-lag；全国供给、天气、季节性、政策和共同测量变化是竞争解释 | 公共因子剔除前后对比、方向稳定性、不可因果声明 |
| G3 单位/约束 | 10 种核心蔬菜；产品内 Tier A 城市；3 日频为主、周频为稳健性；地理候选边、有限滞后 | 覆盖审计、单位测试、候选边数量和滞后契约 |
| G4 基准/识别 | 目标城市自身滞后与公共因子是预测基线；来源城市滞后必须提供额外样本外增益。当前没有因果识别设计 | 连续时间 train/validation/final-test、nested baseline、禁用因果措辞 |
| G5 不确定性 | BH-FDR、时间分窗方向一致性、moving-block bootstrap/置换和 final-test 增益共同约束发布 | 原始/FDR 边数、稳定率、增益分布和降级路线 |
| G6 Pricing 接口 | 输出是成本风险复核的提前信号和范围扩展器，进入 Pricing Engine 上游，不生成零售价 | shock origin/exposure/delay 与复核动作的字段映射 |
| G7 边界 | 历史数据截止 2022-06-22；无销量、流量、天气、产量、库存和真实运输网络 | 页面常驻限制、模型卡、共同冲击替代解释 |

**阶段初始 Gate：** `conditional_pass_to_experiment`。允许建立分析契约和运行历史实验；只有统计校正、稳定性与样本外门槛同时通过，才允许方向传播模块 release。

## 3. 冻结实验契约

### 3.1 样本与时间切分

- 官方产品：`config/p2_scope.yaml` 中冻结的 10 种核心蔬菜；其余 20 种不进入 v0.1 正式结论。
- 官方城市：每种产品 `coverage_tier == A`；Tier B 只允许做覆盖诊断，不参与边选择或发布。
- 训练期：2014-02-02 至 2019-12-31。
- Validation：2020-01-01 至 2020-12-31，用于冻结频率、滞后、最小增益和稳定性阈值。
- Final test：2021-01-01 至 2022-06-22，只使用一次，不重新选择边或阈值。

### 3.2 频率、变量与公共因子

- 主频率：连续 3 日箱，以全局日历起点为锚；每箱至少 2 个有效城市日。
- 稳健性频率：`W-MON` 周频；每周至少 4 个有效城市日。
- 箱内价格：有效 `analysis_price` 的中位数，单位保持 `CNY/kg`。
- 变化变量：相邻箱的 log price change。
- 季节项：只用训练期估计产品×年内位置的稳健中位数变化。
- 全国共同因子：同一产品×时间箱其他 Tier A 城市变化的 leave-one-out 横截面中位数。
- 传播残差：城市变化减训练期季节项和 leave-one-out 共同因子；PCA 只作为稳健性方案，不得利用 final test 拟合载荷。

### 3.3 冲击与候选边

- 正向冲击：城市×产品传播残差超过该系列训练期第 95 百分位；负向冲击只做对称诊断，不进入首版 Pricing 风险输出。
- 阈值只用训练期估计，并原样应用于 validation/final test。
- 来源和目标必须是同产品 Tier A 城市且不同城市。
- 每个目标最多保留 8 个候选来源，并受产品完整有向城市对 35% 上限约束：先按距离选择同省 Tier A 城市，再由最近的其他 Tier A 城市补足；距离来自冻结城市地理维度。
- 地理规则只定义可检验集合，不证明存在真实采购、运输或贸易联系。
- 禁止以全样本相关性、final-test 表现或网络图美观程度筛选候选边。

### 3.4 方向关系、统计与预测基准

- 主模型：目标残差的 distributed-lag OLS。
- 基线：目标城市自身 1–4 个 3 日滞后；完整模型额外加入来源城市 1–4 个滞后。
- 稳健性：周频模型使用 1–2 周滞后。
- 统计证据：训练期来源滞后联合检验，按产品对全部候选边执行 Benjamini–Hochberg，`q <= 0.05`。
- 时间稳定性：训练期前后半段的累计方向一致，且 rolling/moving-block resample 选择率至少 60%。
- Validation 边门槛：相对目标自身滞后基线的 RMSE 改善至少 1%，MAE 不得恶化超过 0.5%，方向保持一致。
- Final-test 确认：validation 冻结边在 final test 的 RMSE 改善大于 0；正式强边要求至少 1%。
- 禁止把统计显著但没有样本外增益的边发布为传播关系。

### 3.5 发布门槛

方向传播模块取得 `network_release` 必须同时满足：

1. 至少 5/10 个产品各有不少于 3 条 final-test 确认边；
2. final-test 中至少 70% 的冻结边 RMSE 改善为正；
3. 冻结边的 final-test RMSE 改善中位数至少 1%；
4. 方向一致性、bootstrap/置换稳定性和 FDR 证据完整；
5. 公共因子剔除后的边数显著少于原始同步网络，并在模型卡解释差异；
6. 页面和文档只使用“预测性传播关系 / lead-lag”，不出现因果贸易流或实时执行承诺。

未通过时发布状态为 `common_shock_only`：只保留共同冲击、城市暴露和同步共振，不展示方向边、传播中心或预计传播路径。

## 4. 任务总览

| 任务 | 内容 | 状态 | 核心产物 |
|---|---|---|---|
| P5-T0 | 冻结契约并做数据/候选边可行性审计 | 已完成 | 配置、审计代码、审计报告、契约测试 |
| P5-T1 | 构建 3 日/周频传播 mart | 待开始 | 公共因子、残差、冲击标签、manifest |
| P5-T2 | 建立目标自身滞后与共同冲击基线 | 待开始 | baseline scorecard、公共因子对比 |
| P5-T3 | 训练候选方向模型并执行 BH-FDR | 待开始 | 候选边、联合检验、validation 结果 |
| P5-T4 | 稳定性、bootstrap/置换与时间分窗验证 | 待开始 | 稳定性记分牌、边降级结果 |
| P5-T5 | 一次性 final-test 与发布决策 | 待开始 | final scorecard、release decision、模型卡 |
| P5-T6 | 构建本地冲击暴露产品数据与页面 | 待开始 | `/propagation`、本地浏览器数据、页面边界 |
| P5-T7 | 全量回归、交付清单与面试叙事 | 待开始 | runbook、delivery checklist、interview guide |

## 5. 任务卡

### P5-T0：契约与可行性审计

**计划动作**

1. 新增冻结配置 `config/p5_propagation_experiment.yaml`；
2. 检查 10 种产品 Tier A 城市、地理坐标、3 日/周频分箱覆盖和 split 可用性；
3. 计算事前地理候选边规模，证明没有退化为全配对检验；
4. 检查 log change、共同因子调整残差和训练期 P95 冲击是否存在足够变异；
5. 输出本地 JSON 审计、公开 Markdown 摘要和自动契约测试；不估计或展示传播边。

**验收标准**

- 10 种产品均有至少 20 个 Tier A 且带坐标城市；
- 每产品候选边不超过完整有向城市对的 35%，且每个目标至少有 5 个、最多 8 个候选来源；
- 训练、validation、final test 的 3 日箱均有足够覆盖；
- 每产品训练期至少有 100 个正向冲击观测，validation/final 各至少 10 个，仅用于可行性而非调阈值；
- 测试锁定时间边界、频率、leave-one-out、候选边与 final-test 隔离；
- G1–G7 保持 `conditional_pass_to_experiment`，无方向网络结论。

### P5-T1：传播 mart

**计划动作**

- 由 Gold 城市日事实生成 3 日与周频面板；
- 用训练期参数构建季节项、leave-one-out 公共因子、传播残差和冻结冲击阈值；
- 生成地理候选边表；
- 写入本地 modeling Parquet/manifest，并建立无前视、单位和键唯一性测试。

**验收标准**

- final-test 信息不参与训练期季节项、阈值或 PCA；
- 所有输出主键唯一、价格为正、频率和最小箱内观测满足配置；
- leave-one-out 共同因子不包含当前城市自身；
- mart 和 manifest 仅保留本机。

### P5-T2：公共冲击与预测基线

**计划动作**

- 比较未调整变化、季节调整变化和公共因子调整残差的同步性；
- 建立目标城市自身滞后基线；
- 冻结 RMSE/MAE 计算、缺失处理和 rolling-origin 预测口径。

**验收标准**

- 报告公共因子剔除前后横截面相关变化；
- 每个产品/split 有可复算 baseline；
- 基线不读取任何来源城市未来值。

### P5-T3：候选方向模型与 FDR

**计划动作**

- 只对冻结地理候选边拟合 distributed-lag 模型；
- 训练期做来源滞后联合检验并按产品执行 BH-FDR；
- validation 比较 nested baseline 的样本外 RMSE/MAE；
- 在 validation 后冻结候选边和全部阈值。

**验收标准**

- 报告总候选数、原始显著数、FDR 后边数和 validation 保留数；
- 没有 final-test 指标参与选择；
- 显著但无预测增益的边被明确淘汰。

### P5-T4：稳定性验证

**计划动作**

- 训练期前后半段方向复核；
- moving-block bootstrap 或等价时间块置换；
- 3 日主频与周频稳健性比较；
- 对不稳定边执行降级。

**验收标准**

- 每条保留边有稳定率、方向一致性和频率敏感性；
- 不稳定边不进入 final-test 冻结集合；
- 多重检验和 resample 次数可由配置复算。

### P5-T5：一次性 final test

**计划动作**

- 对冻结边运行一次 final-test；
- 计算边级、产品级和全局样本外增益；
- 按预设门槛生成 `network_release` 或 `common_shock_only`；
- 输出模型卡并冻结不可声称内容。

**验收标准**

- `threshold_reselected_on_final_test == false`；
- `candidate_edges_reselected_on_final_test == false`；
- 发布决策可由 scorecard 机械复算；
- 未通过时页面不得展示方向网络。

### P5-T6：本地产品集成

**计划动作**

- 新增 `/propagation` 工作面，第一屏回答当前查看的是共同冲击还是已验证 lead-lag；
- 展示来源、暴露、历史延迟、证据状态和 Pricing 复核动作；
- 提供产品、城市、时间窗和证据门槛控件；
- 加入加载、空、错误和 `common_shock_only` 降级状态。

**验收标准**

- 页面不依赖 GitHub 数据；浏览器 JSON 只在本机；
- 不出现“因果传播”“实时预警”“最优价格”或自动执行承诺；
- 与 P1–P4 导航、视觉语言和 Pricing 主线一致；
- 内容测试、构建和关键路由通过，不公开部署。

### P5-T7：交付

**计划动作**

- 完整数据回归、代码作品集 verifier、前端构建；
- 编写 P5 模型卡、runbook、交付清单和面试指南；
- 对 G1–G7 给出最终 `pass/conditional_pass/fail`；
- 更新 GUIDEBOOK 与 README，并只提交代码/计划/方法文档。

**验收标准**

- 所有正式指标有本地 artifact 和测试证据；
- Git 历史无数据、无执行日志；
- 面试叙事明确经济机制、预测证据和识别边界；
- P5 放行结论与页面降级状态完全一致。

## 6. 回滚

- P5 使用独立的 `config/p5_*`、`src/propagation/`、`data/modeling/p5_*`、`artifacts/p5/`、`web/public/data/propagation/` 和 `/propagation`；不覆盖 P0–P4。
- 任一阶段失败时保留审计、公共因子和基线证据，停止后续方向网络发布。
- 删除本地 P5 生成目录即可回到 P4.1；任何删除动作必须另行确认，默认不执行。
