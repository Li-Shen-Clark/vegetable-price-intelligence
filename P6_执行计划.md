# P6 执行计划：Pricing 求职作品集交付

> 阶段目标：把已完成的 P0–P5 转化为招聘方可以在 3–5 分钟内理解、验证和追问的 Pricing / Economics / Data Science 作品集，同时保留代码可审计性与“数据不上传”边界。
> 产品边界：P6 不新增业务模型、不重新打开任何 final test、不改写 P2–P5 发布决定；不上传原始或派生数据、artifacts、浏览器 JSON 或执行日志；不部署网站。

## 1. 执行规则

1. 每个任务先在本计划冻结目标、输入、验收与不可做事项，再执行，再向本地 `P6_执行日志.md` 追加结果。
2. 每个任务复核 Economics & Pricing Gate G1–G7，尤其检查“这项包装是否帮助招聘方理解经济决策和 Pricing 工作流”，而非只增加视觉装饰。
3. P0–P5 的正式结果只读；不得为求职叙事美化失败指标、隐藏基线回退或重新挑选成功案例。
4. GitHub 只包含代码、配置、测试、聚合方法结论和 Markdown/非数据视觉资产；数据、artifacts、CSV/Parquet、浏览器 JSON 与全部执行日志保持本地。
5. 新增自动化必须能在无数据 clone 中运行；不得伪造真实数据来让测试通过。
6. 网站保持本地，不调用托管或部署；若未来公开，需另行授权并重新审查数据与截图边界。

## 2. Economics & Pricing Hypothesis Card

| Gate | P6 冻结答案 | 所需证据 |
|---|---|---|
| G1 决策 | 招聘经理和面试官需要快速判断候选人能否把市场数据转化为可治理的 Pricing 决策支持 | 一页 case study、角色适配、3–5 分钟演示路径 |
| G2 机制 | 作品主线是价格离散、预期、不确定性、交易成本、共同冲击和成本传导；竞争解释与 no-go 必须保留 | 经济机制矩阵、P5 降级案例、不可因果声明 |
| G3 单位/约束 | 统一解释 CNY/kg、时间跨度、距离、成本与历史截止；无数据 clone 和本地完整模式分离 | 架构/数据流、口径清单、数据边界测试 |
| G4 基准/识别 | 每项模型结果都相对冻结基线或情景基准陈述；无识别设计不使用因果语言 | evidence matrix、模型发布状态和反事实列 |
| G5 不确定性 | 展示 partial release、FPR/recall、敏感性与 P5 no-go，而不是只展示最好结果 | 证据漏斗、局限、失败即降级的治理叙事 |
| G6 Pricing 接口 | 明确每个模块进入成本基准、毛利复核、询价或报价审批的字段与动作 | Pricing integration contract 与工作流图 |
| G7 边界 | 数据截止 2022-06-22；无销量、库存、客户、合同、真实物流和实时生产 | 首屏/README/面试脚本一致披露 |

**初始 Gate：** `conditional_pass_to_portfolio_audit`。已有证据足以进入作品集整理，但在无数据模式、CI、全站 lint 和招聘叙事完成前不得标记 P6 完成。

## 3. 任务总览

| 任务 | 内容 | 状态 | 核心产物 |
|---|---|---|---|
| P6-T0 | 仓库、叙事、页面、质量和公开边界审计 | 已完成 | `docs/p6_portfolio_audit.md` |
| P6-T1 | 冻结项目级 case study、证据矩阵和 Pricing 集成契约 | 已完成 | Case Study、evidence matrix、integration contract |
| P6-T2 | 求职材料：简历 bullets、面试答案与 3–5 分钟演示脚本 | 已完成 | Resume pack、interview/demo guide |
| P6-T3 | 构建无数据依赖的 `/case-study` 招聘展示页与社交预览 | 已完成 | 静态 narrative route、OG asset、全站入口 |
| P6-T4 | 代码质量和无数据 CI | 已完成 | 全站 lint 收口、GitHub Actions、code-only tests |
| P6-T5 | 全量验收、文档同步和本地提交 | 已完成 | delivery checklist、最终 Gate、commit |

## 4. 任务卡

### P6-T0：作品集缺口审计

**计划动作**

- 核对 README、GUIDEBOOK、P2–P5 模型卡、页面和 Git 历史的发布状态是否一致；
- 盘点招聘方在无数据 clone 中能看到和不能看到的内容；
- 记录全站 lint、构建、测试、路由、CI、社交预览和演示材料缺口；
- 将缺口按“求职影响×实施成本×证据风险”排序。

**验收标准**

- 审计明确当前最强故事、最大可信度风险和 P6 必做/不做项；
- 不读取或重算任何 final test；
- G1–G7 得到初始状态，数据与执行日志不进入 Git。

### P6-T1：项目级证据架构

**计划动作**

- 编写一页项目 Case Study，先讲决策问题，再讲数据、模型、产品、结果和限制；
- 建立 P0–P5 证据矩阵，逐阶段列用户决策、经济机制、基线、正式指标、发布状态和不可声称内容；
- 定义面向现有 pricing engine 的只读集成契约，字段只到决策输入和人工动作，不定义自动价格执行。

**验收标准**

- P2 partial release、P3 误报、P4 参数假设和 P5 no-go 均可见；
- 集成契约包含状态、证据时间、可靠性、回退路线和人工审批字段；
- 不制造 API 已上线或商业收益已实现的印象。

### P6-T2：求职材料

**计划动作**

- 按 Pricing Analyst、Pricing Data Scientist 和 Economics/Data Scientist 三类岗位编写可量化简历 bullets；
- 编写 30 秒、2 分钟和 5 分钟版本，以及常见技术/业务追问；
- 编写无需公开数据的本地演示脚本，覆盖一个成功模块、一个 partial release 和一个 no-go。

**验收标准**

- 每条 bullet 均可追溯到正式指标，不声称收入、利润或实时生产影响；
- 面试脚本明确项目与 pricing engine 互补；
- 演示在 5 分钟内完成且不依赖展示原始数据文件。

### P6-T3：招聘展示页

**计划动作**

- 新增静态 `/case-study` narrative route，不依赖 `web/public/data`；
- 第一屏回答做了什么、为什么与 Pricing 有关、正式结果和可信边界；
- 展示 P0–P5 决策链、证据状态、技术/经济能力和面试演示入口；
- 生成不含真实数据的品牌社交预览，并接入 metadata；
- 从现有五页加入 Case Study 入口，保留原工作台结构。

**验收标准**

- 全新无数据 clone 仍可打开 `/case-study`；
- 页面不嵌入真实逐行数据、图表截图或浏览器 JSON；
- 加载不依赖外部接口，移动端和桌面端结构清晰；
- 不部署。

### P6-T4：代码质量和 CI

**计划动作**

- 区分生成式 UI 组件规则与产品代码问题，修复或用可解释配置收口全站 lint；
- 新增 GitHub Actions，仅运行无数据契约、全站 lint 和生产构建；
- 确认 workflow 不下载、恢复或上传任何项目数据/artifact。

**验收标准**

- `npm run lint` 和 `npm run build` 通过；
- code-only 测试在现有数据目录暂时不可见时仍可运行；
- CI YAML 不含数据路径、artifact upload 或秘密依赖。

### P6-T5：最终交付

**计划动作**

- 运行 152 项完整本地测试、code-only 测试、全站 lint、生产构建和关键路由；
- 更新 README、GUIDEBOOK 与交付清单；
- 对 G1–G7 给出最终 `pass/conditional_pass/fail`；
- 运行严格 verifier，只提交可公开代码/文档/非数据资产，不 push。

**验收标准**

- 完整模式与无数据作品集模式都有清晰验收证据；
- Git 候选与全部历史无数据、无执行日志；
- 网站保持本地，用户可自行检查后通过 Desktop push；
- P6 的求职叙事与 P0–P5 正式发布状态一致。

## 5. 回滚

- P6 主要新增文档、`/case-study`、社交预览和 CI，不覆盖 P0–P5 数据或模型。
- 任何叙事或页面出现过度承诺时，先撤回该展示资产，不修改底层模型结果来配合文案。
- CI 阻塞现有开发流程时，回退 workflow 与 lint 收口改动；保留审计和求职文档。

## 6. 最终判定

P6 状态为 `complete`，Economics & Pricing Gate 为 `pass`。169/169 项完整本地测试、31/31 项无数据测试、全站 lint、生产构建、`/case-study` 与 `/og.png` HTTP 检查以及严格 Git 发布校验均通过。该结论仅确认作品集交付完成，不改变 P2–P5 的正式模型发布状态，也不授权部署或当前市场使用。
