# P7 执行计划：GitHub Pages 求职版发布

> 阶段目标：把 P6 的无数据 Case Study 发布为英文优先、招聘方可直接访问的 GitHub Pages，同时保持完整分析工作台、本地数据和执行记录不公开。
> 公开地址目标：`https://li-shen-clark.github.io/vegetable-price-intelligence/`

## 1. 执行规则

1. 每个任务先在本计划冻结范围、验收与不可做事项，再执行，并向本地 `P7_执行日志.md` 追加结果。
2. 每一步复核 Economics & Pricing Gate G1–G7；公开页面必须让招聘方先看到决策价值、经济机制、正式证据与边界。
3. 只公开白名单静态文件：HTML、CSS、图片和必要站点元文件；不得包含 CSV、Parquet、JSON 数据、浏览器数据、artifacts、模型文件或执行日志。
4. GitHub Pages 所需的临时部署包只能由白名单目录生成；它不是研究 artifact，且必须先通过内容扫描才能上传。
5. P2–P5 的正式发布状态只读；不得删除 baseline fallback、误报负担、参数假设或 network no-go。
6. 不将五个依赖本地数据的工作台伪装为公开可用产品；公开站只提供 Case Study 与 GitHub 证据入口。
7. 用户已要求“完成 P7”，本阶段包含 GitHub Pages 配置与公开发布；不部署到 Streamlit 或其他平台。

## 2. Economics & Pricing Hypothesis Card

| Gate | P7 冻结答案 | 验收证据 |
|---|---|---|
| G1 决策 | Pricing / Economics / Data Science 招聘方需要在 3 分钟内判断候选人能否把市场数据转成受治理的决策支持 | 英文首屏、结果卡、角色适配与清晰 CTA |
| G2 机制 | 价格离散、预期、不确定性、交易成本、共同冲击和成本传导构成项目主线 | Decision chain 与 Economics section |
| G3 单位/约束 | 明确 CNY/kg、7/14/28 日、117 城、历史截止日和数据缺口；公开包不含逐行数据 | Scope 与 limitations |
| G4 基准/识别 | 预测相对冻结基线，预警固定 FPR，采购是参数情景，传播不作因果解释 | Evidence ladder 与 release labels |
| G5 不确定性 | P2 partial release、P3 误报、P4 敏感性、P5 no-go 是主要治理证据 | Failure/decision governance 模块 |
| G6 Pricing 接口 | 输出只进入成本基准、毛利复核、询价与报价审批；不执行价格或订单 | Pricing Engine handoff 模块 |
| G7 边界 | 数据止于 2022-06-22；无销量、库存、合同、真实物流和商业收益；未上线实时系统 | 首屏、limitations、SEO 描述一致 |

**初始 Gate：** `conditional_pass_to_public_build`。P6 证据完整，但英文公开叙事、静态包安全扫描、正式域名 metadata 和线上可访问性尚未完成。

## 3. 任务总览

| 任务 | 内容 | 状态 | 核心产物 |
|---|---|---|---|
| P7-T0 | 审计公开内容、仓库与 GitHub Pages 前置 | 已完成 | 内容差距、发布边界、Pages 状态 |
| P7-T1 | 构建英文优先的静态 Case Study，补强 ownership、技术能力和招聘 CTA | 已完成 | `portfolio-site/` |
| P7-T2 | 建立发布包白名单、安全扫描和 Pages workflow | 已完成 | verifier、测试、workflow |
| P7-T3 | 全量本地验收、提交、push 和 GitHub Pages 发布 | 已完成 | 公开 URL、Actions 结果 |
| P7-T4 | 线上响应、metadata、内容与最终 Gate 验收 | 已完成 | delivery checklist、最终日志 |

## 4. 任务卡

### P7-T0：公开发布审计

**计划动作**

- 检查现有 `/case-study`、README、P6 材料和站点 metadata 的内容一致性；
- 检查 GitHub 远端、仓库可见性、Pages 状态和可用发布方式；
- 识别招聘内容缺口，优先处理英文可读性、个人贡献、技术栈、结果解释与 CTA；
- 冻结只发布单页 Case Study 的架构，不改写完整工作台。

**验收标准**

- 明确当前最值得补充的内容和不应公开的内容；
- Pages 发布方法不要求提交项目数据或执行日志；
- 不进行页面源代码修改或外部发布，直到本任务日志完成。

### P7-T1：静态招聘页面

**计划动作**

- 建立无框架运行时、无数据请求、相对资源路径的静态页面；
- 使用英文主叙事，保留必要中文项目定位；
- 补充 `My role`、`What I built`、技术能力、决策治理、正式局限和 GitHub CTA；
- 复用现有品牌 OG 图，不生成新的视觉资产。

**验收标准**

- 页面可在仓库子路径和本地静态服务器打开；
- 首屏在 30 秒内回答问题、规模、结果、Pricing 关系和边界；
- 不含真实城市/产品逐行数据、动态图表数据或对完整工作台的失效公开链接。

### P7-T2：发布安全与 CI

**计划动作**

- 新增 Pages bundle verifier，对文件扩展名、路径、大小、本机路径和禁用词进行白名单校验；
- 新增 P7 契约测试，验证内容、metadata、经济学/Pricing 边界和无数据包；
- 新增 GitHub Pages workflow，在发布前运行 verifier 与 code-only 测试。

**验收标准**

- workflow 只上传 `portfolio-site/` 白名单静态包；
- 无 CSV、Parquet、JSON、数据目录、日志、source map 或秘密；
- workflow 权限仅为 Pages 发布所需最小权限。

### P7-T3：发布

**计划动作**

- 运行完整本地测试、code-only 测试、web lint/build、静态站 HTTP 与严格 Git 校验；
- 本地提交 P7，push 到 `origin/main`；
- 启用或确认 GitHub Pages 的 Actions 发布源并等待 deployment 完成。

**验收标准**

- 所有测试与安全校验通过；
- Git 历史和部署包均无数据/执行日志；
- Actions deployment 成功，公开 URL 可访问。

### P7-T4：线上验收与内容结论

**计划动作**

- 验证正式 URL、OG 图片、canonical、标题和移动端 viewport 响应；
- 更新 README、公开案例、Stage Gate 和 P7 delivery checklist；
- 给出已完成优化、仍可选增强和最终 G1–G7 判定。

**验收标准**

- 线上页面与本地白名单包一致；
- P7 Gate 为 `pass`，且不提升任何底层模型结论；
- 不再存在必须完成的公开发布任务。

## 5. 回滚

- 若 Pages 公开包不符合数据边界，立即停止部署，不通过修改 `.gitignore` 放行。
- 若线上发布失败，保留已验证静态包和 workflow，记录外部阻塞；不得改用 Streamlit 或上传数据绕过。
- 若内容出现过度承诺，撤回相关公开措辞并重新发布；不得改变正式模型结果配合叙事。

## 6. 最终 Gate

P7 Gate：`pass`。

- GitHub Pages 正式地址：`https://li-shen-clark.github.io/vegetable-price-intelligence/`；
- Actions verify 与 deploy 成功，HTTPS 首页和四类站点资源均返回 200；
- 177 项全量测试、39 项 code-only 测试、lint、production build、Pages verifier 与 portfolio verifier 均通过；
- 公开内容明确连接经济机制、Pricing 决策和个人 ownership，同时完整保留 P2–P5 的降级、误报、参数假设与 no-go；
- Pages 只发布无数据静态 Case Study，五个分析工作台、数据、模型产物与执行日志仍不公开。

## 7. P7.1 仓库展示元数据

> 目标：让招聘方从 GitHub 仓库列表和 About 区域直接识别项目主题并进入 Portfolio，不改变代码、模型结论或公开数据边界。

| 字段 | 冻结值 | 验收 |
|---|---|---|
| Description | `Historical price forecasting, risk alerts, model governance, and pricing intelligence across 8.68M wholesale-market records.` | 公开仓库 About 可见且措辞与 Case Study 一致 |
| Website | `https://li-shen-clark.github.io/vegetable-price-intelligence/` | 可点击并指向正式 Portfolio |
| Topics | `pricing`, `forecasting`, `economics`, `time-series`, `model-governance`, `python` | 六项均在公开仓库页显示 |

执行边界：只编辑 GitHub repository metadata；不上传文件，不修改可见性、代码、Issues、Actions、Pages source 或数据排除规则。

验收结果（2026-09-14）：Description、Website 与六个 Topics 已在公开仓库首页逐项复核，P7.1 状态为 `pass`。

## 8. P7.2 Portfolio 个人署名

> 目标：让招聘方在首屏直接识别项目作者及岗位定位，不改变既有决策叙事和证据层级。

**冻结文案**

```text
Built by
Li Shen
Applied Economist · Pricing & Decision Science
```

**实现与验收**

- 放在英文 Hero 的项目介绍与主要按钮之间，作为清晰但不抢夺主标题的署名块；
- 延续绿色、琥珀色和衬线姓名层级，桌面与移动端均保持三行可读；
- 同步 HTML author metadata，并把精确文案加入 P7 契约测试；
- Pages 静态包、数据排除和 P2–P5 正式结论均不改变。

验收结果（2026-09-14）：署名块、author metadata、8 项 P7 契约与两类发布边界校验均通过，P7.2 本地状态为 `pass`；等待 GitHub Pages 自动发布后完成线上复核。

## 9. P7.3 Evidence Dashboard

> 目标：用真实、冻结、可解释的模型评估图提升专业性，同时遵守“不公开数据”的硬边界。

**图表范围**

1. **Forecast release by horizon**：7/14/28 日各 10 个产品的 `baseline_fallback`、`point_only_model`、`model_minimum`、`model_target` 构成；
2. **Alert stability: validation → final**：Average Precision、Recall 与 FPR 的 validation/final 两点稳定性曲线；
3. **Propagation evidence funnel**：2,736 个候选、991 个可估计、15 个冻结关系、4 个 final 正增益、0 个公开方向关系。

**发布边界**

- 只使用 model card/release artifact 已公开的冻结汇总指标，不加入城市、蔬菜或日期级价格序列；
- 不绘制虚构趋势线，不把情景参数画成真实业务结果，不公开可还原原始报价的数据；
- 使用静态、可访问的 HTML/CSS/inline SVG，不增加 JavaScript、数据文件、第三方依赖或网络请求；
- 图注必须说明 lower/higher is better、固定 FPR 预算和 network no-go，不能只展示“漂亮曲线”。

**验收**

- 三张图在宽屏为三列、窄屏为单列，文本与数值在无脚本环境可读；
- P7 契约测试覆盖标题、关键值和无行级数据声明；
- Pages/portfolio verifier、8 项 P7 测试与 Git 差异校验通过；
- GitHub Pages 自动部署后，线上 HTML 与三图标题可访问。

验收结果（2026-09-14）：三张静态图、响应式布局、可访问文本、8 项 P7 契约与两类发布校验均通过；P7.3 本地状态为 `pass`，等待 GitHub Pages 发布后完成线上复核。
