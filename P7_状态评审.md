# P0–P7 状态评审与建议

评审日期：2026-09-15
方式：核对 Git 状态、release decision JSON、交付清单、`.gitignore` 与公开仓库树，而非只读日志。

---

## 1. 现状

| 维度 | 实测 |
|---|---|
| 代码 | 45 个 Python 模块，13,588 行 |
| 测试 | 177 个测试函数，36 个测试文件 |
| 本地产品 | 5 条路由：`/`、`/forecast`、`/alerts`、`/procurement`、`/propagation`，另有 `/case-study` |
| 公开产出 | GitHub Pages 静态单页 + Public 仓库（263 个文件，其中 54 份 docs） |
| 公开包 | `portfolio-site/` 6 个白名单文件，约 1.98 MB，无 JS、无数据请求 |

### 各阶段发布状态

| 阶段 | 状态 | 关键数字 |
|---|---|---|
| P0 数据层 | 完成 | 8,682,381 行；246 市场映射 / 23 未映射；117 城市 |
| P1 Monitor | 完成 | 历史回放看板 |
| P2 预测 | `partial_release` | 30 组中：模型+区间达标 1、最低可用 3、仅点预测 16、**基线回退 10** |
| P3 预警 | `alert_release` | PR-AUC 0.2204（基线 0.0775，+184%）；FPR 9.99%；recall 37.69%；precision 21.31% = 3.18× prevalence |
| P4 采购 | `scenario_release` | 843 行 Tier A 快照；36 组参数网格 × 1,080 行敏感性；实际跨度 28 日 |
| P5 传播 | **`common_shock_only`** | 2,632 候选对 → FDR 1,481 边 → 共同因子调整后 776 边（−47.6%）→ 冻结 15 → final 确认 4 → **发布 0**；中位 RMSE 改善 **−1.39%** |
| P6 作品集 | 完成 | Case study、evidence matrix、demo script |
| P7 公开站 | 完成 | Pages 上线，仓库改为 Public，About 元数据、署名、Evidence Dashboard |

---

## 2. 最重要的判断：P5 的 no-go 是本项目最有价值的结果

首次评审时我指出传播网络有两个方法论硬缺陷——不剔除全国共同因子会让任意两城市都 Granger 显著；不做多重检验校正，纯噪声也会产出大量"显著"边。

P5 把两项都做了，结果是：

- 共同因子调整直接砍掉 **47.6%** 的边（1,481 → 776）
- 冻结的 15 条边在 final test 只确认 4 条，正向边占比 26.7%（门槛 70%）
- 中位样本外 RMSE 改善是 **负的**（−1.39%，门槛 +1%）

于是发布 0 条方向性边，状态定为 `common_shock_only`。

**这不是失败，是用正确方法论得到的诚实否定结论。** 大多数同类作品集项目会跳过校正、放一张漂亮的网络图上去。这个项目做了校正、拿到负结果、并照实发布——它比一张假网络图更能证明能力。建议把它当方法论卖点前置，而不是当缺憾放在漏斗末端（见 §4.2）。

---

## 3. 🔴 唯一紧急项：GUIDEBOOK 撤回没有真正完成

### 3.1 事实

1. 撤回提交 `f33e531`（`Keep product guidebooks private`）**尚未 push**。此刻 `origin/main` 的文件树里 `GUIDEBOOK.md` 与 `GUIDEBOOK_可实施性评审.md` **仍然公开可读**。
2. 即使推送成功，这两份文件仍存在于已推送的历史中：
   - `GUIDEBOOK.md`：`f9a22d6`、`3bae439`、`ea90f91`、`c2f8fb4`
   - `GUIDEBOOK_可实施性评审.md`：`c2f8fb4`
   仓库现在是 Public，任何人 `git log -- GUIDEBOOK.md` 即可取回全文。
3. 撤回计划自己写明"本轮不改写 Git 历史；若需清除旧提交中的历史版本，必须另行确认"。**现在就是需要确认的时点。**

### 3.2 先回答一个问题：撤回到底要保护什么？

`.gitignore` 只排除了三份 GUIDEBOOK 相关文件和 `*执行日志.md`。而 **P0–P7 的 9 份 `执行计划.md` 全部是公开的**，它们包含与 GUIDEBOOK 同源的规格：事件定义、时间切分、发布门槛、参数网格、模型候选。

所以：

- 如果动机是"产品规格不外传" → **撤回是不完整的**，9 份阶段计划泄露的信息量与 GUIDEBOOK 相当，必须一并处理。
- 如果动机只是"这份文档不够成熟、不想以它作为项目门面" → 目标基本达成，但历史仍可取回，**要么接受，要么改写历史**。

### 3.3 三个选项

| 选项 | 动作 | 代价 |
|---|---|---|
| A. 接受现状 | push `f33e531` 即可；承认历史可取回 | 零成本；但撤回只是"不再展示"，不是"不可获取" |
| B. 彻底清除 | `git filter-repo` 重写历史 + force push | 已 fork/clone 的副本仍有旧版；GitHub 上的悬空对象在 GC 前仍可按 commit SHA 访问，需联系 GitHub Support 清除；仓库只有你一人协作，所以协作代价低 |
| C. 重建仓库 | 新建干净仓库，只推最终状态 | 最彻底；代价是丢失全部提交历史——而提交历史恰恰是这个项目治理质量的证据 |

**建议：先定动机，再选。** 若属于 §3.2 的第二种（多数情况），选 A 并把 9 份阶段计划也一并评估是否要留——它们对招聘方其实是加分项，展示了"计划先行、冻结后执行"的工作方式。

**无论选哪个，第一步都是 push `f33e531`**，因为现在的状态是"已决定撤回但实际仍公开"，是三种状态里最差的一种。

---

## 4. 两个影响招聘效果的问题

### 4.1 🟡 代码公开但数据不公开 → 外部无法运行任何东西

`.gitignore` 排除 `data/*`、`artifacts/`、`web/public/data/`；公开页也按设计删除了指向五个本地工作台的链接。

外部可见的只有：一个静态叙事页 + 代码 + 54 份文档。

这是数据合规上的正确选择，但后果是**项目最强的部分——五个交互式工作台和完整的冻结/回测治理链路——没有任何外部可验证的证据**。招聘方只能"读你说做了什么"，不能"看你做了什么"。

三个成本递增的补救：

| 成本 | 动作 | 效果 |
|---|---|---|
| 低（半天） | 60–90 秒录屏，逐页演示 5 个工作台 | 唯一能让外部看到工作台的途径。P7 日志把它列为"可选增强"——它不是可选的 |
| 中（2–3 天） | 造完全合成的小样本（10 城 × 3 产品 × 2 年），让仓库 `make demo` 能跑 | 招聘方能亲手跑通管道；必须显著标注 synthetic |
| 高（1 周） | 把 `/procurement` 做成脱敏静态版发到 Pages | 它最直观，且是参数化情景、不暴露真实价格序列 |

**优先做第一个。**

### 4.2 🟡 Evidence Dashboard 把方法论胜利画成了产出归零

P7.3 的三张图里，P5 是 2,736 → 991 → 15 → 4 → **0** 的漏斗。招聘方扫一眼，记住的是最后那个 0。

建议改造：

- 标题从"证据漏斗"改成类似 *"How statistical correction eliminated 99.9% of spurious relationships"*
- **把共同因子调整的 1,481 → 776 这一步放进图里**——这是整张图里最能体现方法论能力的一步，现在完全没出现
- 在图注里写清楚：没有这两步校正，这张网络会"成功"发布上千条边，而它们是噪声

同一组数字，换个叙事框架，从"我没做出来"变成"我知道怎么不被数据骗"。

---

## 5. 三个小项

1. **文档与实现不一致**：GUIDEBOOK §8 的 MVP 写的是 7/14 日预测，但实测 7 日 8/10 基线回退、平均改善 −4.2%，而 P4 实际用的是 28 日。建议把 MVP 跨度正式改成 **14/28 日**并说明理由——这是面试时容易被追问的不一致点。
2. **存量 lint**：`npm run lint` 全站仍有 P4 之前就存在的问题（P4 清单已披露）。仓库已公开，招聘方可能会跑一次。半天可清。
3. **Actions Node.js 20 deprecation warning**：升 action major version，10 分钟。

---

## 6. 总结

这个项目的工程与治理质量已经明显高于典型作品集项目：冻结时间切分、阈值只在 validation 选定、final test 一次性打开且不回头调参、分级发布而非全有全无、负面结果照实公布、每阶段都有可复算的契约测试。P5 的诚实 no-go 和 P2 的 partial release 是两个最有说服力的信号。

真正的短板不在技术，而在**外部可见性**：公开面只有一张静态页，最能证明能力的五个工作台和 177 个测试的治理链路，外部完全看不到。

现在的优先级应该从"继续加阶段"转向三件事：

1. **把 GUIDEBOOK 撤回做干净**（先 push `f33e531`，再定要不要处理历史和 9 份阶段计划）
2. **录一个 90 秒演示视频**，这是投入产出比最高的一项
3. **重写 Evidence Dashboard 的 P5 叙事**，把方法论胜利讲成方法论胜利

这三件加起来不超过两天，对项目对外说服力的提升远大于再做一个 P8。
