# P4 采购情景引擎交付清单

> P4-T6 已于 2026-09-14 完成。本清单只把 `scenario_release` 解释为“历史参数化情景可本地演示”。

## A. 数据与方法

- [x] 117 城坐标与 `dim_city` 一一对应，来源、方法和大理特殊规则可追溯。
- [x] 旧市场坐标完整率和偏差已审计，正式距离不读取旧市场坐标。
- [x] 10 产品×3 跨度逐组继承 P2 发布路线，计数保持 1/3/16/10。
- [x] 843 行采购快照均为 Tier A，主键唯一。
- [x] 99 行使用已发布区间风险，744 行使用历史发布路线残差；未发布区间保持为空。
- [x] 30 组残差校准最晚目标日 2022-04-29，早于 2022-05-01 情景起点。
- [x] 843/843 行数据可靠性精确连接，默认门槛和有界乘数已版本化。
- [x] 到岸成本、本地基准、Top 3、并列排序和参数校验均有独立测试。
- [x] 36 组参数网格生成 1,080 行敏感性明细。

## B. 产品与披露

- [x] `/procurement` 支持 117 个目标城市、10 种产品和 7/14/28 日。
- [x] 模型点预测、基线点价格、合格区间和经验残差风险有不同显示语义。
- [x] `baseline_fallback` 明确写明“不是模型预测”。
- [x] 本地基准与外地候选分开；无本地基准不伪造节省。
- [x] 展示点价格、风险、距离、运输、损耗、单位到岸和总成本拆分。
- [x] 展示进入 Top 3 与第一名频率，而非只给单一确定排名。
- [x] 加载、错误、无候选、无本地基准和参数越界状态已实现或测试。
- [x] 常驻显示 Historical scenario、数据截止日、距离假设和“不执行采购”。
- [x] P1/P2/P3/P4 四页导航已连通。

## C. 可复现与交付

- [x] `config/p4_procurement.yaml` 冻结全部参数与路径。
- [x] mart、Web 数据和敏感性 artifact 含行数/哈希 manifest。
- [x] `docs/p4_runbook.md` 提供从零重建、分步验收、排错与回滚。
- [x] `docs/p4_interview_guide.md` 提供求职叙事、演示路径和证据边界。
- [x] `README.md`、公开案例文档、`docs/data_dictionary.md` 已同步实际实现。
- [x] P0–P4 全量 Python 测试 101/101 通过，其中 P4 新增 29 项。
- [x] P4 采购页定向 lint 通过。
- [x] 生产构建最终复跑通过并识别 `/`、`/forecast`、`/alerts`、`/procurement`。
- [x] 四条页面路由与 metadata、样例产品 JSON 最终复跑均返回 HTTP 200。
- [x] 未初始化 Git、未提交、未推送、未公开部署。

## D. 放行规则

只有 A–C 必需项均完成，才可标记 `scenario_release`。若坐标、发布路线或无前视风险失败则为 `blocked`；若引擎可运行但候选覆盖/稳定性不足则为 `research_only`。

最终放行状态：**`scenario_release`**。

历史验收说明：P4 交付时，全项目 `npm run lint` 曾报告 P4 开工前即存在于旧页面和通用组件的存量规则问题；P4 新增 `/procurement` 当时通过定向 lint 和完整生产构建。该技术债随后已在 P6 全站质量收口中修复，当前主分支完整 `npm run lint` 为 0 错误。
