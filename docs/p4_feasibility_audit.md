# P4 可行性审计

## 结论

状态：`pass`。117 城均有唯一城市锚点，P2 发布矩阵和 2022-05-01 快照可以支撑参数化采购情景。旧市场坐标已从正式距离计算中隔离。

## 核心检查

- 城市锚点：117 行，117 个唯一城市；直接城市中心 116 行，大理县级特殊锚点 1 行。
- 市场映射：`mapped_existing_city` 246 行，`unmapped_new_city` 23 行；正式市场坐标缺失 46 行。
- P2 发布矩阵：`model_target` 1、`model_minimum` 3、`point_only_model` 16、`baseline_fallback` 10。
- 快照预测：843 行，覆盖 10 种产品、41 个城市、3 个跨度；每产品来源城市范围 21–36。
- 敏感性网格：36 个组合。

## 放行边界

本审计只放行 P4 数据准备和情景引擎开发。所有输出仍是 Historical scenario；不得把城市中心距离称为公路里程，不得把基线价格称为模型预测，也不得把参数化排名称为真实供应建议。
